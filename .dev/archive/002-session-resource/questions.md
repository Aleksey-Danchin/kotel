# Q&A Round 1

## Q1 — Пункт 2: Контракт `signin` (вход по логину)

Сейчас в backend есть только `UsersController` с `GET /api/users`, а ресурса `session` пока нет (`apps/backend/src/users/users.controller.ts`, `apps/backend/src/app.module.ts`). В `User` есть поля `login` и `passwordHash`, поэтому нужно зафиксировать точный формат входа и ответа для `signin`, чтобы шаги были однозначными.

**Варианты:**
- a) `POST /api/session/signin` с body `{ "login": string, "password": string }`, ответ `204 No Content`
- b) `POST /api/session/signin` с body `{ "login": string, "password": string }`, ответ `200` и объект пользователя
- c) Используем другой путь/метод/поля (уточни)
- d) Другое: ___

**Ответ:** `POST /api/session/signin` с body `{ "login": string, "password": string }`, ответ `200` и объект пользователя. Для входа будем использовать zod. Zod нужно будет установить ТОЛЬКО В BACKEND. Frontend будет его экспортировать по alias. Нужно будет зафиксировать в документации, что ZOD это наш основной и (на данный момент) единственный dto способ.

---

## Q2 — Пункт 2: Контракты `signout`, `check`, `get session user`

В тексте есть 4 действия (`signin`, `signout`, `check`, `get session user`), но не до конца ясно, какие из них отдельные endpoint, а какие только внутренние методы (например, `get session user` через guard/decorator). Чтобы не создать лишние endpoint, нужно уточнение API-контрактов.

**Варианты:**
- a) Отдельные endpoint: `POST /session/signout`, `GET /session/check`; `get session user` только через `@SessionUser()`
- b) Отдельные endpoint: `POST /session/signout`, `GET /session/check`, `GET /session/user`
- c) Нужны другие пути/методы (опиши)
- d) Другое: ___

**Ответ:** `POST /session/signin` (тут возвращается пользователь, если удалось авторизоваться), `POST /session/signout`, `GET /session/check` (тут возвращается пользователь, если сессия активна);

---

## Q3 — Пункт 3: Настройки session-cookie

В `main.ts` сейчас не подключен cookie parser и не задан CORS/credentials; backend только выставляет `app.setGlobalPrefix('api')`. Нужно зафиксировать параметры cookie, иначе поведение между dev/test может быть разным.

**Варианты:**
- a) Имя cookie `session`, `httpOnly: true`, `secure: true`, `sameSite: 'lax'`, `path: '/api'`
- b) Имя cookie `session`, `httpOnly: true`, `secure: true`, `sameSite: 'none'`, `path: '/api'`
- c) Нужны другие параметры (включая `domain`/`maxAge`)
- d) Другое: ___

**Ответ:** Имя cookie `session`, `httpOnly: true`, `secure: true`. Домен рабочий. Создадим переменную окружения для выставления имени домена. В режиме разработки соответственно kotel.localhost. maxAge не нужно, domain kotel.localhost/api/*

---

## Q4 — Пункты 4 + 5: Продление и `IDLE_TIMEOUT`

В `Session` уже есть `lastUsedAt`, что подходит под sliding idle timeout, но нужно строго определить момент продления и сравнение таймаута (`>=`/`>`). Это влияет и на guard, и на `check`.

**Варианты:**
- a) Продлевать на каждом успешном использовании сессии (guard/check), обновляя `lastUsedAt = now`
- b) Продлевать только на `check`, а в guard только валидация
- c) Не продлевать при `check`, только при защищенных endpoint
- d) Другое: ___

**Ответ:**  Продлевать на каждом успешном использовании сессии (guard/check), обновляя `lastUsedAt = now`

---

## Q5 — Пункт 6: Политика количества сессий на пользователя

Сейчас в модели `Session` нет ограничений, позволяющих одновременные сессии. В пункте 6 сказано "не помечать, просто удалять", но не уточнено: новая авторизация должна удалять старые сессии пользователя или нет.

**Варианты:**
- a) Разрешить несколько активных сессий на одного пользователя
- b) При новом `signin` удалять все старые сессии пользователя и оставлять одну
- c) Ограничивать по-другому (опиши)
- d) Другое: ___

**Ответ:** Сделайем слабое ограничение по количество активных сессий. Пока это будет харкодом = 10. При создание 11 из бд будет удаляться самый поздний (НЕ САМЫЙ РАННИЙ) из уже имеющихся у пользователей session. Тестировать это нужно.

---

## Q6 — Пункт 7: Где и как запускать периодическую очистку

Пункт формулирует "в конструкторе модуля session", но в Nest обычно безопаснее lifecycle (`onModuleInit`) + `setInterval`/`Scheduler`. Нужно зафиксировать ожидаемый подход и требования к shutdown (очистка таймера).

**Варианты:**
- a) Строго в конструкторе `SessionModule` (как указано)
- b) В `SessionService` через lifecycle (`onModuleInit`) + очистка таймера в `onModuleDestroy`
- c) Через `@nestjs/schedule` cron (если добавляем зависимость)
- d) Другое: ___

**Ответ:** Строго в конструкторе `SessionModule` через setInterval и onModuleInit и onModuleDestroy. Это тоже надо будет протестировать. В vitest можно управлять "часами" окружения для таких тестов?

---

## Q7 — Пункт 8: Семантика `SessionGuard` и HTTP-ответы

Ты привел 3 сценария: default guard, `strong: true`, `strong: false`. Нужно точно закрепить поведение для отсутствующей/битой/просроченной cookie: какой HTTP-код, и когда `sessionUser` может быть `null`.

**Варианты:**
- a) Default = `strong: true`; без валидной сессии -> `401`; при `strong: false` пропускаем и даем `null`
- b) Default = `strong: false`; только `strong: true` блокирует с `401`
- c) Иная логика (опиши подробно для каждого режима)
- d) Другое: ___

**Ответ:**  Default = `strong: true`; без валидной сессии -> `401`; при `strong: false` пропускаем и даем `null`

---

## Q8 — Пункт 9: Lazy `getSessionUser` в `req` и поведение кэша

Нужно уточнить детали для `req.getSessionUser`: должен ли результат мемоизироваться в пределах запроса (чтобы 2 вызова не делали 2 SQL-запроса), и что возвращать если пользователь удален, а session запись есть.

**Варианты:**
- a) Да, мемоизация внутри запроса; при отсутствии пользователя возвращать `null`
- b) Без мемоизации; каждый вызов делает запрос
- c) Мемоизация + при отсутствии пользователя бросать `UnauthorizedException`
- d) Другое: ___

**Ответ:** Да, мемоизация внутри запроса; при отсутствии пользователя возвращать `null`. UnauthorizedException в целом нужна будет, если не будет обнаружен сам метод getSessionUser. Тест нужен. НО тестирование должно быть не на основной кодовой базе, потому в основной кодовой базе @SessionUser долежн встречаться только в связке с guard.

---

## Q9 — Пункт 10: Объем endpoint-тестов для session

Сейчас backend-тесты (`*.spec.ts`) mostly мокают Prisma factory, реальных DB интеграций нет (`users.integration.spec.ts` тоже на mock). Для шагов важно зафиксировать: тесты для `signin/signout/check` делаем с mock Prisma или в тестовом compose с реальной БД.

**Варианты:**
- a) Тесты endpoint на mock Prisma (в текущем стиле)
- b) Тесты endpoint в test-compose с реальной БД + seed
- c) Смешанный вариант (какие именно endpoint в каком режиме?)
- d) Другое: ___

**Ответ:** Тесты endpoint в test-compose с реальной БД + seed. НО это не та же самая база, что используется в dev режиме. Для теста поднимается своя база данных, проводиться миграция и сид.

---

## Q10 — Пункт 11: Тестовая страница session на frontend

Сейчас есть `/users` страница и базовый роут `/` (`apps/frontend/src/routes`). Нужно уточнить, где разместить временную страницу и какой минимальный UI нужен для ручной проверки.

**Варианты:**
- a) Отдельный роут `/session-test` с тремя кнопками: `signin`, `signout`, `check` + блок результата
- b) Добавить в текущий `/users` экран временный блок session-теста
- c) Сделать на `/` вместо `Hello World`
- d) Другое: ___

**Ответ:** Отдельный роут `/session-test` с тремя кнопками: `signin`, `signout`, `check` + блок результата

---

## Q11 — Пункт 12: Как внедрять `jotai` на этом раунде

В пункте сказано "jotai будет основной state store", но пока в приложении нет глобального state кроме TanStack Query. Нужно определить минимальный объем внедрения сейчас.

**Варианты:**
- a) Только установить зависимость и базово подключить (без реального использования)
- b) Сразу хранить session user/state (например, результат `check`) в jotai atoms
- c) Полностью перенести текущие/новые состояния на jotai где уместно
- d) Другое: ___

**Ответ:** Сразу хранить session user/state (например, результат `check`) в jotai atoms, но в рамках тестовой страницы.

---

## Q12 — Пункт 13: Структура `api` и `queryOptions` директорий

Сейчас API-вызовы лежат в `src/global/usersApi.ts`. Ты просишь 2 каталога (`api`, `queryOptions`) и разбиение по ресурсам. Нужна конкретика по целевой структуре и месту в дереве `src`.

**Варианты:**
- a) `src/api/<resource>.ts` и `src/queryOptions/<resource>.ts` (плоско)
- b) `src/api/<resource>/*` и `src/queryOptions/<resource>/*` (поддиректории на ресурс)
- c) В другом месте (например `src/shared/api`, `src/shared/queryOptions`)
- d) Другое: ___

**Ответ:** `src/api/<resource>.ts` и `src/queryOptions/<resource>.ts` (плоско)

---

## Q13 — Пункт 15: Где фиксировать правило "Vitest + Playwright only"

В репозитории сейчас нет `RULES.md`, `AGENTS.md` или `.cursor/rules/*`. Есть только общие README (template). Чтобы выполнить пункт, нужно определить конкретные файлы документов/правил.

**Варианты:**
- a) Создать корневой `AGENTS.md` и зафиксировать policy там
- b) Создать `.cursor/rules/testing.mdc` (или аналогичный rule-файл) + кратко в `README.md`
- c) Только `README.md` (корневой или app-level)
- d) Другое: ___

**Ответ:** Создать `.cursor/rules/testing.mdc`. Нужно будет добавить описание.
---

## Q14 — Пункт 16: Детали test-compose окружения

Сейчас есть только `infra/compose/dev.yml`. Для test-окружения ты указал отличия (префикс `test`, без persist volume, без studio, свой traefik+ssl), но есть противоречие с пунктом 19, где studio "должна отрабатывать при старте тестового сервера" для миграции/сидов.

**Варианты:**
- a) В `test.yml` нет long-running `studio` сервиса, но отдельный одноразовый контейнер/команда для migrate+seed перед тестами
- b) В `test.yml` есть `studio` сервис, но без экспонирования UI; используется только для migrate+seed
- c) `studio` полностью исключаем и выполняем migrate+seed другим образом (например через backend/prisma контейнер)
- d) Другое: ___

**Ответ:** В `test.yml` есть `studio` сервис, но без экспонирования UI; используется только для migrate+seed

---

## Q15 — Пункты 17 + 18 + 19: Скрипты тестового контура

Сейчас есть только `dev-start.sh`, `dev-stop.sh`, `prisma-migrate.sh`, `prisma-seed.sh`. Нужно зафиксировать точные имена и контракт нового запуска тестов: что именно делает `test-start.sh`, `test-stop.sh`, и "run all tests" скрипт для steps-man/step-imp.

**Варианты:**
- a) `test-start.sh` поднимает test-compose + migrate+seed, `test-stop.sh` останавливает только test-compose, `test-run.sh` гоняет backend/frontend тесты
- b) Только `test-start.sh` и `test-stop.sh`, а запуск тестов встроен в `test-start.sh`
- c) Нужны другие имена/сценарий (опиши точно команды и порядок)
- d) Другое: ___

**Ответ:** `test-start.sh` поднимает test-compose + migrate+seed, `test-stop.sh` останавливает только test-compose, `test-run.sh` гоняет backend/frontend тесты

---

## Q16 — Пункт 20: Поведение просроченной session

Нужно формально закрепить серверную реакцию: при попытке использовать просроченную session ты хочешь удалять cookie у клиента. Важно уточнить HTTP-статус и распространяется ли удаление на `check` и guard strong-mode одинаково.

**Варианты:**
- a) Всегда очищать cookie (`Set-Cookie` с истечением), возвращать `401` для strong/check
- b) Очищать cookie и для `check` возвращать `200 { user: null }`, а для strong-endpoint `401`
- c) Очищать cookie, но коды/ответы другие (уточни)
- d) Другое: ___

**Ответ:**  Всегда очищать cookie (`Set-Cookie` с истечением). Дальше по абстоятельствам и настройке гарда.

---

## Q17 — Пункт 21: Формат отчета по backend `*.spec.ts` и политика удаления

Сейчас в backend 4 spec-файла, часть из них выглядит как шаблонные (`app.controller.spec.ts`) или smoke-проверки конфигурации (`prisma.factory.definition.spec.ts`). Нужно понять целевой формат отчета и уровень "агрессивности" удаления.

**Варианты:**
- a) Отчет как markdown-документ + удаляем явно неактуальные тесты в этом же раунде
- b) Отчет как markdown-документ, ничего не удаляем без отдельного подтверждения
- c) Добавить статус по каждому тесту (реальный/генерированный/нужен/удалить позже), но удаления отложить
- d) Другое: ___

**Ответ:** Отчет не нужен. удаляем явно неактуальные тесты в этом же раунде. Кажется, что все существующие тесты не актуальные, так как до сих пор не было ранудов, включающие тестирование.

# Q&A Round 2

## Q18 — Уточнение к Q3: `domain` и `path` у cookie

В ответе к Q3 указано `domain kotel.localhost/api/*`, но в cookie это два отдельных параметра: `domain` (только хост) и `path` (путь). Чтобы шаги не были с неверным контрактом, зафиксируй точные значения.

**Варианты:**
- a) `domain: kotel.localhost`, `path: /api`
- b) `domain` не задаем вообще, только `path: /api`
- c) `domain: .kotel.localhost`, `path: /api`
- d) Другое: ___

**Ответ:** `domain: kotel.localhost`, `path: /api`

---

## Q19 — Уточнение к Q5: лимит 10 сессий и какую удалять

Ты указал: при создании 11-й удалять "самый поздний (НЕ САМЫЙ РАННИЙ)". Это нестандартно, поэтому нужно явно закрепить сортировку и область лимита.

**Варианты:**
- a) Лимит **на пользователя**: при 11-й удаляем сессию с максимальным `lastUsedAt` (самую свежую из старых)
- b) Лимит **на пользователя**: при 11-й удаляем сессию с минимальным `lastUsedAt` (самую старую)
- c) Лимит **глобальный по всей таблице Session** (уточни какую удалять)
- d) Другое: ___

**Ответ:** Лимит **на пользователя**: при 11-й удаляем сессию с максимальным `lastUsedAt` (самую свежую из старых). А точнее проверка есть ли устаревшие, удалить устаревшие, итогда все норм. НО если 10 активных сессия, то удаляется самая молодая. Да, это не стандартно, но нужно именно так.

---

## Q20 — Уточнение к Q16 + Q7: коды ответа для `check` и guard

В Q16 ты зафиксировал обязательное удаление просроченной cookie, а HTTP-коды — "по обстоятельствам и настройке гарда". Для step-файлов нужен точный behavior matrix.

**Варианты:**
- a) `GET /session/check`: всегда `200`, при невалидной/просроченной сессии возвращает `null`; `SessionGuard strong:true/default`: `401`; `strong:false`: пропускает с `sessionUser=null`
- b) `GET /session/check`: `401` при невалидной/просроченной сессии; guard как в Q7 (`strong:true/default` => `401`, `strong:false` => pass)
- c) `check` тоже зависит от режима guard и может быть разным (опиши)
- d) Другое: ___

**Ответ:** `GET /session/check`: всегда `200`, при невалидной/просроченной сессии возвращает `null`; `SessionGuard strong:true/default`: `401`; `strong:false`: пропускает с `sessionUser=null`

---

## Q21 — Уточнение к Q1: трактовка "Zod только в backend, frontend экспортирует по alias"

Фраза в ответе к Q1 двусмысленна: если Zod установлен только в backend, frontend не сможет импортировать zod-схемы напрямую без общей зависимости/пакета. Нужно зафиксировать архитектуру DTO/схем.

**Варианты:**
- a) Zod-схемы живут в backend и используются только там (frontend не импортирует их)
- b) Zod-схемы выносятся в shared-пакет/папку, frontend и backend используют их обе стороны (тогда зависимость нужна и frontend)
- c) Frontend не использует Zod, но получает типы/контракты другим способом (опиши)
- d) Другое: ___

**Ответ:**  Zod-схемы живут в backend, но используются при необхдимости на Frontend и на backend. Frontend нормально импортирует zod схемы, если в backend все зависимости правильно установлены. Проверено.

---

## Q22 — Уточнение к пунктам 10/14/15: Jest -> Vitest в backend

Сейчас backend тесты запускаются через Jest (`apps/backend/package.json`), но ты хочешь закрепить правило "только Vitest + Playwright". Нужно подтвердить, надо ли в этом раунде переводить backend endpoint-тесты на Vitest.

**Варианты:**
- a) Да, в этом раунде полностью уходим от Jest в backend и пишем endpoint-тесты на Vitest
- b) Пока оставляем Jest в backend, правило Vitest+Playwright зафиксируем "на будущее"
- c) Смешанный переход: новые endpoint-тесты уже на Vitest, старые Jest удаляем
- d) Другое: ___

**Ответ:** Да, в этом раунде полностью уходим от Jest в backend и пишем endpoint-тесты на Vitest. jest зависимости удаляем.

# Q&A Round 3

## Q23 — Финальное подтверждение перед генерацией шагов

Я собрал достаточную детализацию по всем пунктам suggestion и готов переходить к генерации `.dev/steps/*.md`. Перед этим нужно финальное подтверждение: добавлять ли еще какой-то контекст/ограничения в текущий раунд.

**Варианты:**
- a) Подтверждаю, можно генерировать шаги
- b) Есть еще детали, допишу ниже
- c) Нужно исключить часть пунктов через `<SKIPPED>`
- d) Другое: ___

**Ответ:** Подтверждаю, можно генерировать шаги
