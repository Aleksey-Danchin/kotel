# Q&A Round 1

## Q1 — Пункты 1–4: Формат параметра в адресной строке

Сейчас роут чата в `apps/designer/src/routes/~$chatId/~index.tsx` ожидает path-параметр `/$chatId/`, а query-параметры для выбора сервера/чата не используются. В тексте задачи говорится про «параметр в адресной строке», который может быть и `serverId`, и `chatId`. Нужно зафиксировать точный формат URL, чтобы корректно реализовать разбор и навигацию.

**Варианты:**
- a) Один path-параметр: `/:id` (в нем может быть либо `serverId`, либо `chatId`)
- b) Query-параметр: `/?id=...`
- c) Два query-параметра: `/?serverId=...&chatId=...` (или один из них)
- d) Другое: ___

**Ответ:** Один path-параметр: `/:id` (в нем может быть либо `serverId`, либо `chatId`). Не будет ли проблема реализовать такое с tanstack/router?

---

## Q2 — Пункты 2–3: Поведение на «чистом» первом запуске

В `apps/designer/src/state/store.ts` сейчас есть `DEFAULT_SERVER_URL = "https://kotel.localhost"` в `selectedServerUrlAtom`, а в `apps/designer/src/state/servers.ts` `initialActiveServerUrl` также выбирает первый сервер по умолчанию. Это конфликтует с требованием «если параметра нет и в localStorage пусто — ничего не выбрано». Нужно подтвердить целевое поведение для первого открытия.

**Варианты:**
- a) На первом запуске всегда `ничего не выбрано` (даже если есть дефолтные сервера в моках)
- b) Оставляем текущий автоселект первого сервера
- c) Автоселект только при наличии единственного сервера
- d) Другое: ___

**Ответ:** На первом запуске всегда `ничего не выбрано` (даже если есть дефолтные сервера в моках). DEFAULT_SERVER_URL было временным решением.

---

## Q3 — Пункты 2, 6, 8, 9: Точная схема localStorage

Сейчас хранится один общий `selectedChatIdAtom` (`kotel.designer.selectedChatId`) и один `selectedServerUrlAtom`. По задаче нужен «последний чат на сервер» + очистка и store/localStorage при Esc. Нужно определить структуру ключей и уровень совместимости с текущими.

**Варианты:**
- a) Новый ключ-мапа вида `kotel.designer.lastChatByServerId = { [serverId]: chatId }`, старый `selectedChatId` больше не использовать
- b) Отдельные ключи на сервер: `kotel.designer.lastChat.<serverId>`
- c) Оставить текущий `selectedChatId`, но дополнять новой структурой для per-server
- d) Другое: ___

**Ответ:** Новый ключ-мапа вида `kotel.designer.lastChatByServerId = { [serverId]: chatId }`, старый `selectedChatId` больше не использовать

---

## Q4 — Пункты 3, 5: Пустое состояние при неизвестном параметре

Сейчас при неверном `chatId` в `apps/designer/src/routes/~$chatId/~index.tsx` показывается сообщение: «Чат не найден для этого сервера или неверный адрес». В задаче указано, что при несоответствии параметра «ничего не отображается. В том числе и ошибка». Нужно зафиксировать, что именно рендерим в основной области и в колонках.

**Варианты:**
- a) Полностью пустая main-область (без текстов), список серверов виден, список чатов пуст
- b) Нейтральная заглушка без ошибки (например, «Выберите сервер»), список чатов пуст
- c) Оставляем текущую ошибку про неверный адрес
- d) Другое: ___

**Ответ:** Полностью пустая main-область (без текстов), список серверов виден, список чатов пуст

---

## Q5 — Пункты 3–4, 7: Приоритет при совпадении id

В моках есть независимые `server.id` и `chat.id`. Теоретически возможна коллизия (одна строка совпадает и с `serverId`, и с `chatId`). Для предсказуемого роутинга нужно определить приоритет разрешения параметра.

**Варианты:**
- a) Сначала проверять `chatId`, затем `serverId`
- b) Сначала `serverId`, затем `chatId`
- c) Считать это ошибкой данных и показывать пусто
- d) Другое: ___

**Ответ:** А это интересно. Давай так. Мы точно знаем, что chatId это cuid. А serverId на самом деле это псевдоним для domain. Например serverId=kotel.localhost. Так что конфликта не должно быть.

---

## Q6 — Пункты 6–9: Логика Esc и обратной навигации

Сейчас в `apps/designer/src/routes/~__root.tsx` обработчик Esc делает: из чата -> `navigate("/")` + `selectedChatId = null`; из сервера -> `selectedServerUrl = null`. По задаче нужно: из чата переходить к параметру `serverId`, а из сервера убирать параметр полностью, плюс чистить «последний чат сервера». Нужна точная спецификация переходов.

**Варианты:**
- a) Esc в чате: URL становится `/:serverId`, Esc на сервере: URL становится `/`
- b) Esc в чате: URL становится `/?id=<serverId>`, Esc на сервере: URL становится `/`
- c) Esc в чате: URL сразу `/` (текущий подход), только очистка состояния меняется
- d) Другое: ___

**Ответ:** Esc в чате: URL становится `/:serverId`, Esc на сервере: URL становится `/`

---

## Q7 — Пункты 13, 17: Имитация загрузки и вывод сообщений

Сейчас `getChatMessages(chatId)` есть в `apps/designer/src/state/store.ts`, но на странице чата в `apps/designer/src/routes/~$chatId/~index.tsx` стоит заглушка «Тут будет список сообщений». Нужно определить, где именно показывать skeleton/loader и на какие события (переключение сервера, переключение чата, первый вход).

**Варианты:**
- a) Лоадер только при открытии/переключении чата
- b) Лоадер и при выборе сервера, и при выборе чата
- c) Лоадер только на первом заходе после reload
- d) Другое: ___

**Ответ:** Лоадер и при выборе сервера, и при выборе чата.

---

## Q8 — Пункты 14, 17: Формат сообщения и визуальное различие сторон

В модели `ChatMessage` сейчас есть `author`, `text`, `createdAt`, но нет явного `direction`/`isOutgoing`. Для «легкой цветовой разницы» нужно понять правило: по какому признаку сообщение считается «от пользователя» и «пользователю».

**Варианты:**
- a) Добавляем в мок-модель поле направления (`direction: "outgoing" | "incoming"`)
- b) Вычисляем направление по `author` (например, по `session.user.login`/`fullname`)
- c) Все существующие сообщения incoming, новые отправленные — outgoing
- d) Другое: ___

**Ответ:** Вычисляем направление по `author` = `session.user.id`

---

## Q9 — Пункты 18–19: Поведение формы отправки

Требуется поле ввода + кнопка + отправка по Enter, с добавлением автоответа через ~2 секунды. Нужно закрепить UX-детали: многострочность, очистка поля, блокировки, и как именно формируется автоответ.

**Варианты:**
- a) `textarea`: Enter отправляет, Shift+Enter переносит строку
- b) `input`: Enter отправляет, переносы не поддерживаются
- c) Кнопка и Enter отправляют только непустой trimmed текст; после отправки поле очищается
- d) Автоответ дословно дублирует текст пользователя через 2 секунды
- e) Другое: ___

**Ответ:** `textarea`: Enter отправляет, Shift+Enter переносит строку. Обязательный trim форматирование перед отправкой. Пустые сообщения после форматирования не отправляем.

---

## Q10 — Пункт 15: Обогащение мок-данных

Сейчас в `apps/designer/src/state/mocks.json` всего 6 сообщений и покрыты не все чаты. Если добавлять полноценный UI сообщений/диалогов, вероятно нужно расширить моки (авторы, временные метки, распределение по чатам). Нужно определить минимально достаточный объем изменений моков в этом проходе.

**Варианты:**
- a) Минимально: только то, что нужно для текущего UI (без больших расширений)
- b) Средне: заполнить сообщениями все существующие чаты
- c) Расширенно: добавить дополнительные поля/сценарные данные для будущих фич
- d) Другое: ___

**Ответ:** Средне: заполнить сообщениями все существующие чаты. Для некоторых чатов сделать достаточное колиество сообщения для значимого скроллинга. Это тоже будет тестироваться.

---

## Q11 — Пункт 16: Объем e2e-покрытия

Задача просит «простые e2e тесты». Нужен точный список обязательных сценариев. От этого зависит декомпозиция шагов (роутинг/state и UI отправки сообщений могут идти раздельно).

**Варианты:**
- a) Минимум: выбор сервера/чата, Esc-навигация, синхронизация URL <-> state
- b) Плюс сообщения: рендер списка, отправка по кнопке, отправка по Enter, автоответ
- c) Плюс persistence: восстановление из localStorage после reload
- d) Другое: ___

**Ответ:** так, ладно, тестов не надо. Тесты будут когда будем переносить в frontend.

---

## Q12 — Пункт 20: «Общая оценка» и рефакторинг

В пункте 20 запрошена оценка и возможный рефакторинг бизнес-логики. Чтобы корректно разложить шаги, нужно понять ожидаемый результат: отдельный аналитический документ, отдельный технический шаг с рефакторингом, или только короткие замечания в notes.

**Варианты:**
- a) Сделать отдельный step на аудит + целевой рефакторинг
- b) Только включить замечания/риски в `Notes` соответствующих шагов
- c) Подготовить отдельный markdown-отчет в `.dev/` без обязательной реализации
- d) Другое: ___

**Ответ:** Только включить замечания/риски в `Notes` соответствующих шагов

---

## Q13 — Пункт 11: Правая граница колонки чатов

В `apps/designer/src/routes/ChatsColumn.tsx` уже есть `border-r` у `<aside>`: `border-r border-base-300`. Похоже, пункт уже выполнен. Подтвердите, нужно ли что-то менять дополнительно (цвет/толщина/условное отображение) или этот пункт считаем закрытым.

**Варианты:**
- a) Да, пункт уже закрыт — можно не включать в шаги
- b) Нужна другая граница (уточню параметры ниже)
- c) Нужна граница в другом месте/layout-обертке
- d) Другое: ___

**Ответ:** Удалить ее там. Добавить в ~__root.tsx для main элемента. И сделать ее заметной с учетом цветов назанчений daisyui.

---

Еще:
1) Сама страница должна быть без прокрутки. НО каждая из колонок должна иметь область для прокрутки при достаточном количестве контента.
2) У каждой колонки должны быть фиксированные зоны в заголовке и в подвале.
3) У колонки с серверами должен быть зафиксированны заголовок: там надпись "Сервера".
4) У колонки с серверами должен быть зафиксированны подвал: там кнопка "Добавить сервер".
5) У колонки с чатами должен быть зафиксированны заголовок: там будет название сервера.
6) У колонки с чатом, должен быть зафиксированны заголовок: там будет имя собеседника или называние чата для групповых чатов. Групповых чатов пока нет.
7) У колонки с чатом, должен быть зафиксированны подвал: там будет форма для сообщений.
8) Дополнительно в во все заголовки: если выбранные сервер совпадает с адресом web-клиента и пользователя роль ADMIN или ROOT, то дополнительно шестеренка. Прямо сейчас при клике ничего не будет происходить, нужно чтобы была такая икнока кнопка.

# Q&A Round 2

## Q14 — Пункты 1, 4, 6 + уточнение к Q1: Роутинг `/:id` и существующие статические страницы

В `apps/designer/src/routes` уже есть статические роуты (`/setup`, `/session-test`, `/users`, `/callback`) и динамический `/$chatId/`. При переходе на единый `/:id` важно явно зафиксировать, что статические пути остаются приоритетными и не воспринимаются как id. Технически в TanStack Router это реализуемо: статические роуты имеют более высокий приоритет, но лучше закрепить как требование.

**Варианты:**
- a) Да, `/setup|/session-test|/users|/callback` должны работать как сейчас, `/:id` только для server/chat
- b) Нет, любые пути кроме `/` и `/:id` больше не нужны
- c) Другое: ___

**Ответ:** Да, `/setup|/session-test|/users|/callback` должны работать как сейчас, `/:id` только для server/chat

---

## Q15 — Пункты 1, 4, 6: Формат `serverId` в URL

Вы уточнили, что `serverId` — это псевдоним домена (например `kotel.localhost`), а не внутренний `srv_*`. В моках есть сервер `http://127.0.0.1:8081`; такой идентификатор содержит `:` и может потребовать отдельной нормализации для path. Нужно зафиксировать канонический формат id в URL.

**Варианты:**
- a) `serverId = hostname` (без порта), например `127.0.0.1`
- b) `serverId = host` (с портом), например `127.0.0.1:8081`
- c) Использовать отдельный slug/alias (без `:`), хранимый в моках
- d) Другое: ___

**Ответ:** `serverId = host` (с портом), но вместо : будет точка `127.0.0.1.8081`.

---

## Q16 — Пункты 2, 3, 9: Что именно хранить как «последний активный сервер»

По требованиям нужно «забывать последний активный сервер из store и localStorage». Чтобы сделать стабильный маппинг URL<->state, нужно выбрать, что считается persisted идентификатором сервера.

**Варианты:**
- a) Храним `serverId` (доменный alias), не `serverUrl`
- b) Храним `serverUrl` как сейчас, а `serverId` только для роутинга
- c) Храним оба (`serverId` + `serverUrl`)
- d) Другое: ___

**Ответ:** Храним `serverId` (доменный alias), не `serverUrl`

---

## Q17 — Пункт 8: Очистка last-chat при Esc из чата

Вы зафиксировали: Esc в чате -> `/:serverId` и забываем last chat для этого сервера. Нужно подтвердить, что очистка происходит только при Esc, а не при любом переходе с чата на сервер (например кликом по серверу/назад в браузере).

**Варианты:**
- a) Очищать только на Esc из чата
- b) Очищать при любом выходе из чата на уровень сервера
- c) Никогда не очищать автоматически, кроме явного reset
- d) Другое: ___

**Ответ:** Очищать при любом выходе из чата на уровень сервера

---

## Q18 — Пункты 186–193 («Еще»): Фиксированные зоны и скролл контейнеры

Сейчас root/layout использует `min-h-screen`, а у колонок нет явного разбиения на `header / scroll-body / footer`. Нужно зафиксировать целевую схему, чтобы шаги были однозначны.

**Варианты:**
- a) Весь viewport без общего скролла страницы; прокрутка только внутри scroll-body каждой из 3 колонок
- b) Только боковые 2 колонки скроллятся внутри себя, main может скроллить страницу
- c) Оставить общий page-scroll и добавить только sticky-заголовки
- d) Другое: ___

**Ответ:** Весь viewport без общего скролла страницы; прокрутка только внутри scroll-body каждой из 3 колонок

---

## Q19 — Пункты 191–193: Заголовок/подвал колонки чата (main-область)

В текущем коде третья колонка — это `<main>` с контентом route, отдельного компонента «ChatColumn» нет. Для фиксированных header/footer нужно решить, где размещать эти элементы архитектурно.

**Варианты:**
- a) Создать общий shell для чата в `~__root.tsx` (header/footer там), а route-страницы рендерят только тело сообщений
- b) Держать header/footer внутри `~$chatId/~index.tsx`
- c) Вынести в отдельный компонент (например `ChatColumn.tsx`) и использовать из root
- d) Другое: ___

**Ответ:** Вынести в отдельный компонент (например `ChatColumn.tsx`) и использовать из root

---

## Q20 — Пункт 194: Условие показа иконки-шестеренки в заголовках

Требование: показывать шестеренку во всех заголовках, если выбранный сервер совпадает с адресом web-клиента и роль пользователя ADMIN/ROOT. В `ServerSession.user.role` сейчас значения в моках: `admin`, `operator`, `viewer`, `developer` (нижний регистр и нет `root`). Нужно зафиксировать нормализацию ролей и правило сравнения адресов.

**Варианты:**
- a) Сравнение по `window.location.hostname === serverId`; роль сравниваем case-insensitive (`admin|root`)
- b) Сравнение по `window.location.host` (с портом); роль строго `ADMIN|ROOT`
- c) Порог роли временно только `admin` (пока root в моках нет)
- d) Другое: ___

**Ответ:** Сравнение по `window.location.hostname === serverUrl` (serverUrl|serverHost - нужно будет выбрать правильный); роль сравниваем case-insensitive (`admin|root`). Вообще в моделе указано: 

enum UserRole {
  USER
  ADMIN
  ROOT
}


---

## Q21 — Пункт 194 + пункт 15: Изменения моков под роль `ROOT` и подписи чата

Чтобы реально проверить условие для шестеренки и заголовок чата («имя собеседника» для 1-1), возможно нужно расширить моки: добавить хотя бы одного пользователя `root` и метаданные чата (тип/собеседник). Нужно подтвердить объем.

**Варианты:**
- a) Добавляем `root`-пользователя в один из серверов + минимальные поля для имени собеседника
- b) Не меняем моки ролей, проверяем только `admin`
- c) Добавляем явные поля типа чата (`kind`, `peerName`) уже сейчас
- d) Другое: ___

**Ответ:** Добавляем `root`-пользователя в один из серверов + минимальные поля для имени собеседника

---

## Q22 — Пункты 14, 17, 18: Заголовок чата и подпись для текущих (не групповых) чатов

Вы указали: в заголовке колонки чата для негрупповых чатов должно быть «имя собеседника». Сейчас в моках есть только `title/subtitle`. Нужно выбрать правило отображения до появления полноценной модели собеседника.

**Варианты:**
- a) Использовать `chat.title` как имя собеседника временно
- b) Добавить в моки отдельное поле `peerName` и использовать его
- c) Показывать `chat.subtitle`/комбинацию title+subtitle
- d) Другое: ___

**Ответ:** Давай добавим в мок chat.type="person|group". И будет опциональный title. Сооветственно будет выводить имя собеседника при person, и title при group. Фактически пока групповых нет, но прямо сейчас заложить это в дизнайн можно.

# Q&A Round 3

## Q23 — Уточнение к Q15 + Q20: единое правило serverId и сравнения для шестеренки

Сейчас есть два условия:  
1) `serverId` в URL = `host`, где `:` заменяется на `.` (пример: `127.0.0.1.8081`),  
2) шестеренка показывается, если «выбранный сервер совпадает с адресом web-клиента».  
Чтобы избежать неоднозначности в реализации, нужно одно строгое правило сравнения:
- либо сравниваем с `window.location.hostname` (без порта),
- либо с `window.location.host` (с портом),
- и как при этом сопоставлять с dot-форматом `serverId`.

**Варианты:**
- a) Каноника: сравнение по hostname (без порта), `serverId` тоже без порта
- b) Каноника: сравнение по host (с портом), `serverId` хранит host c заменой `:` -> `.`
- c) Для сравнения шестеренки используем исходный `serverUrl` (hostname/host), а `serverId` dot-формат только для роута
- d) Другое: ___

**Ответ:** Для сравнения шестеренки используем исходный `serverUrl` (hostname/host), а `serverId` dot-формат только для роута

---

## Q24 — Финальное подтверждение перед генерацией step-файлов

Я собрал контекст по всем пунктам и готов перейти к генерации implementation steps в `.dev/steps/*.md`. Есть ли еще детали/ограничения, которые обязательно нужно учесть до генерации?

**Ответ:** Вроде бы все хорошо. Давай к шагам.
