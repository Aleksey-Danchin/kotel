1) Речь пойдет про параметр в адресной строке.

2) Если параметра нет, то из localstorage достается последний сохраненный параметр и применяется. Если в localstorage нет про это данных, то строка остается пустой.

3) Если параметра нет и теперь, то не выбран ни один сервер и ни один чат. НО если есть авторизованные сервера, то они видны. Список чатов соответственно пустой.

3) Если параметр есть, и он соответствует какому-то serverId, то этот сервер автоматически выбран.

4) Если параметр есть, и он соответствует какому-то chatId, то выбраны автоматически эти чат и сервер.

5) Если параметр ничему не соответствует то, о чем известно, то также ничего не отображается. В том числе и ошибка.

6) Когда из списка серверов выбирается сервер, то нужно найти в localstorage последний активный chatid для этого сервера. Если он есть chatId и сам chat найден, именно chatid подставляется в адресную строку. Дальше по сценарию выше.

7) Когда из списка чатов выбирается чат, то именно он подставляется в параметр.

8) Когда выходят из чата, например с помощью Esc, то параметр заменяется на serverId, а последний активный чат для этого сервера забывается и из store и из localstorage.

9) Когда выходят из сервера, например с помощью Esc, то параметр вообще убирается, а последний активный сервер забывается и из store и из localstorage.

10) Нужно адаптировать к этому подходу и designer store и designer route.

11) Нужно добавить правую границу к столбцу с чатами.

12) Речь сейчас идет именно о apps/designer

13) Нужна имитация загрузки данных.

14) Нужен компонент для сообщений, самый базовый только с текстом, датой отправки и легкой цветовой разнице для разделения тех, что от пользователя, и тех что пользователю.

15) Возможно нужно будет пересмотреть и обогатить мок данные.

16) Нужно написать простые e2e тесты на эти сценарии.

17) Нужно вывести сообщения.

18) Нужно добавить в низ чата поле для написания сообщения, кнопку отправить. Но чтобы сообщения так же уходили по клику enter.

19) Отправлять данные в текущий state, построенный на mock. Можно чтобы через 2 секунд добавлялся и ответ с дублирование текст.

20) Нужно дать общую оценку тому, что уже написано. Возможно некоторые бизнес логики элементы требуют рефакторинга.
