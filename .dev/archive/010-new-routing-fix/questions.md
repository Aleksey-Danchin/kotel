# Q&A Round 1

## Q1 — Пункт 1: Пустая средняя колонка в режиме config

В `apps/designer/src/routes/~__root.tsx` в режиме конфигурации сейчас рендерится пустой блок фиксированной ширины (`w-70`) перед `SettingsOverlay`, из-за этого визуально остаётся «пустая колонка» между серверами и настройками. В формулировке пункта сказано, что настройки должны занимать две правые колонки. Нужно зафиксировать точное целевое поведение layout.

**Варианты:**
- a) В режиме `/config*` полностью убрать пустую колонку, а `SettingsOverlay` растянуть на всё пространство правее колонок серверов (2/3 экрана shell).
- b) Оставить 3-колоночную сетку, но заменить пустую колонку содержимым (уточнить каким).
- c) Сделать другую пропорцию ширин (указать точные размеры/классы Tailwind).
- d) Другое: ___

**Ответ:** В режиме `/config*` полностью убрать пустую колонку, а `SettingsOverlay` растянуть на всё пространство правее колонок серверов (2/3 экрана shell).

---

## Q2 — Пункт 2: Возврат выпадающего списка сервера в заголовок настроек

В `apps/designer/src/components/SettingsOverlay.tsx` выпадающий выбор сервера уже есть только для `mode="overlay"`, а для `mode="route"` сейчас показывается только badge с именем сервера. Судя по пункту, dropdown нужно вернуть именно в route-режим. Нужно уточнить ожидаемую механику при выборе сервера.

**Варианты:**
- a) В route-режиме вернуть тот же dropdown, что и в overlay, и при выборе сервера делать переход на `/config/$serverId`.
- b) Вернуть dropdown визуально, но менять только локальный выбранный сервер без смены URL.
- c) Вернуть dropdown с иной логикой (опишите).
- d) Другое: ___

**Ответ:**  В route-режиме вернуть тот же dropdown, что и в overlay, и при выборе сервера делать переход на `/config/$serverId`.

---

## Q3 — Пункт 5: Стрелка «назад» в заголовке настроек

Сейчас в `SettingsOverlay` (route mode) нет кнопки возврата, и выход возможен только через навигацию/переключение маршрута отдельно. Нужно уточнить точку назначения для новой стрелки «влево» в начале заголовка.

**Варианты:**
- a) Всегда возвращать на `/$serverId` (сервер без выбранного чата).
- b) Возвращать на последний открытый чат `/$serverId/$chatId`, если он есть, иначе на `/$serverId`.
- c) Всегда возвращать на `/` (корень).
- d) Другое: ___

**Ответ:** Возвращать на последний открытый чат `/$serverId/$chatId`, если он есть, иначе на `/$serverId`.

---

## Q4 — Пункт 6: Повторный клик по шестерёнке как выход из конфигурации

Сейчас `ColumnHeaderGear` всегда вызывает переход в `/config` или `/config/$serverId` через `resolveConfiguratorNavigationTarget`, и повторный клик в config-режиме не закрывает его. Нужно зафиксировать правило toggle для повторного нажатия.

**Варианты:**
- a) Если уже в `/config*`, повторный клик по любой шестерёнке возвращает в обычный chat-режим для активного сервера.
- b) Toggle работает только для шестерёнки в той колонке, из которой был вход (source-sensitive).
- c) Повторный клик не должен выходить; пункт 6 трактуем иначе (опишите).
- d) Другое: ___

**Ответ:** Если уже в `/config*`, повторный клик по любой шестерёнке возвращает в обычный chat-режим для активного сервера.

---

## Q5 — Пункты 5 + 6: Сохранение контекста вкладки при входе/выходе configurator

Сейчас `ColumnHeaderGear` при каждом клике принудительно задаёт `settingsInitialTabAtom/settingsActiveTabAtom` от `source` (`services -> main`, `chats -> configurator`, `chat -> users`). Если делаем toggle-выход и обратный вход, нужно понять, нужно ли сохранять последнюю вкладку или каждый раз ресетить по source.

**Варианты:**
- a) Оставить текущее поведение: каждый вход в configurator сбрасывает вкладку по source.
- b) Запоминать последнюю открытую вкладку и восстанавливать её при повторном входе.
- c) Смешанное правило (например, первый вход по source, дальше запоминание).
- d) Другое: ___

**Ответ:** Оставить текущее поведение: каждый вход в configurator сбрасывает вкладку по source.

---

## Q6 — Пункт 3: Где именно закрепить правило «designer без тестирования»

Сейчас глобальное правило в `.cursor/rules/testing.mdc` требует Vitest для всех non-E2E тестов. Также есть `.cursor/rules/designer.mdc`, где описано, что `apps/designer` — песочница, но нет явного запрета/исключения по тестам. Нужно уточнить целевую формулировку и место фиксации правила.

**Варианты:**
- a) Добавить исключение в `.cursor/rules/testing.mdc` + дублировать в `.cursor/rules/designer.mdc`.
- b) Менять только `.cursor/rules/testing.mdc`.
- c) Менять только `.cursor/rules/designer.mdc`.
- d) Другое: ___

**Ответ:** Менять только `.cursor/rules/designer.mdc`. В `.cursor/rules/testing.mdc` должен быть scope на frontend и backend.

---

## Q7 — Пункт 4: Объём удаления тестов из `apps/designer`

В `apps/designer/src` сейчас есть набор тестов (`*.test.ts(x)`), включая `SettingsOverlay.test.tsx`, `routePath.test.ts`, `store.test.ts` и др. Нужно уточнить, удаляем ли только файлы тестов, или также чистим всё связанное (скрипты, конфиги, упоминания в документации/архивных шагах, если встречаются).

**Варианты:**
- a) Удалить только `*.test.ts`/`*.test.tsx` внутри `apps/designer/src`; остальное не трогать.
- b) Удалить тесты + убрать/адаптировать test-команды и конфиги в `apps/designer`, чтобы не подразумевалось их наличие.
- c) Не удалять физически, а пометить как `<SKIPPED>` (для истории) и менять только rules.
- d) Другое: ___

**Ответ:** Удалить тесты + убрать/адаптировать test-команды и конфиги в `apps/designer`, чтобы не подразумевалось их наличие.

---

## Q8 — Пункт 7: Состав «общего списка» в колонке чатов

Сейчас `apps/designer/src/routes/ChatsColumn.tsx` рендерит два блока: сначала `filteredChats`, затем секцию «Пользователи» (`filteredUsers`) с отдельными карточками. Для объединения в один список нужно определить модель элементов.

**Варианты:**
- a) В общий список включаем только существующие чаты (group + person); пользователей без уже созданного чата не показываем.
- b) Включаем и существующие чаты, и пользователей без чата как отдельные элементы (клик создаёт/открывает личный чат).
- c) Включаем только пользователей; групповые чаты исключаем.
- d) Другое: ___

**Ответ:** Включаем и существующие чаты, и пользователей без чата как отдельные элементы (клик создаёт/открывает личный чат).

---

## Q9 — Пункт 7: Правило сортировки по «последнему сообщению»

В текущей модели `store.ts` время сообщений берётся из `getChatMessages(chatId)` (поле `createdAt`), но для некоторых чатов/пользователей сообщений может не быть. В пункте указано: «по последнему сообщению убыванию; без сообщений — по алфавиту». Нужно зафиксировать tie-breakers.

**Варианты:**
- a) Сначала элементы с сообщениями (по убыванию `lastMessage.createdAt`), затем элементы без сообщений (по алфавиту `title/fullname`).
- b) Один общий компаратор: если у элемента нет сообщений, считаем его `lastMessage=null` и сортируем после всех с сообщениями; внутри равных — алфавит.
- c) Иной порядок (например, важнее unread/онлайн-статус) — опишите.
- d) Другое: ___

**Ответ:**  Сначала элементы с сообщениями (по убыванию `lastMessage.createdAt`), затем элементы без сообщений (по алфавиту `title/fullname`).

---

## Q10 — Пункт 7: Контент карточки элемента в объединённом списке

Сейчас `ChatCard` и `UserCard` имеют разный формат: у чатов есть unread badge и `subtitle`, у пользователей — статус online/last seen. После объединения возможны разные UX-варианты, и это повлияет на переиспользование компонентов.

**Варианты:**
- a) Оставить два визуальных типа карточек в одном списке (chat-стиль для чатов, user-стиль для пользователей).
- b) Привести все элементы к единому виду карточки (укажите желаемые поля: имя, last message, статус, unread и т.д.).
- c) Использовать стиль `ChatCard` для всего списка, а user-элементы адаптировать под него.
- d) Другое: ___

**Ответ:** Нужно разработать общий ChatCard компонент с возможность описывать его компоновку через рендер пропсы.

---

# Q&A Round 2

## Q11 — Уточнение к Q6: какие именно rule-файлы менять

В ответе к Q6 указано одновременно «менять только `.cursor/rules/designer.mdc`» и «в `.cursor/rules/testing.mdc` должен быть scope на frontend и backend». Это две разные стратегии: либо правим только designer-rule, либо также ограничиваем global testing-rule. Нужно зафиксировать однозначно.

**Варианты:**
- a) Меняем оба файла: `.cursor/rules/testing.mdc` (сужаем до frontend/backend) + `.cursor/rules/designer.mdc` (явно запрещаем тесты в designer).
- b) Меняем только `.cursor/rules/testing.mdc`; `designer.mdc` не трогаем.
- c) Меняем только `.cursor/rules/designer.mdc`; `testing.mdc` не трогаем.
- d) Другое: ___

**Ответ:** Меняем оба файла: `.cursor/rules/testing.mdc` (сужаем до frontend/backend) + `.cursor/rules/designer.mdc` (явно запрещаем тесты в designer).

---

## Q12 — Уточнение к пункту 4: что считать «тестовой инфраструктурой designer»

По пункту 4 вы выбрали удаление тестов и адаптацию test-команд/конфигов в `apps/designer`. Чтобы шаги были точными, нужно определить допустимый уровень удаления.

**Варианты:**
- a) Удаляем только `*.test.ts(x)` и test-скрипты из `apps/designer/package.json`; общерепозиторные конфиги не трогаем.
- b) Удаляем `*.test.ts(x)` + локальные test-скрипты + локальные vitest-конфиги в `apps/designer` (если есть), но не меняем root-пайплайны монорепы.
- c) Полностью вычищаем всё, что ссылается на designer-тесты, включая root-level команды/доки/CI (где встречается).
- d) Другое: ___

**Ответ:** Удаляем `*.test.ts(x)` + локальные test-скрипты + локальные vitest-конфиги в `apps/designer` (если есть), но не меняем root-пайплайны монорепы. общерепозиторные конфиги не трогаем. Но вообще нужно проверить, если что ссылается на designer-тесты, включая root-level команды/доки/CI - вообще не должно быть. Designer сразу был песочницей.

---

## Q13 — Уточнение к пункту 7: состав полей в unified ChatCard

Вы выбрали общий `ChatCard` с настраиваемой компоновкой через render props. Для step-спецификации нужно зафиксировать обязательный UI-контент карточки для обоих типов элементов (чат и «пользователь без чата»), иначе реализация может сильно расходиться.

**Варианты:**
- a) Обязательные поля для всех: title, subtitle, статус (online/offline), unread badge (если >0), метка/превью последнего сообщения (если есть).
- b) Для чатов: как сейчас (`title/subtitle/unread`), для пользователей без чата: `fullname + presence`; без обязательного превью сообщения.
- c) Единая минималистичная карточка: только title + одна вспомогательная строка; без unread/status иконок.
- d) Другое: ___

**Ответ:** Отдаю тебе на рассмотрение этот вопрос для принятия решения как будет лучше. Возможно имеет смысл сделать более-менее универсальный компонент и 2 частных компонента, переиспользующий общий с описанием обязательных параметров.

---

## Q14 — Финальное подтверждение перед генерацией шагов

Все рабочие пункты suggestion разобраны, remaining ambiguity вынесена в Q11–Q13. После ваших ответов я смогу зафиксировать итоговую декомпозицию и сгенерировать `.dev/steps/*.md`.

**Ответ:** 

---

# Q&A Round 3

## Q15 — Подтверждение старта генерации шагов

Все неоднозначности по содержанию пунктов разобраны. По Q13 беру на себя архитектурное решение: выделить общий базовый компонент карточки и два специализированных адаптера (для существующего чата и для пользователя без чата), чтобы сохранить читаемость и избежать перегруженных render-props.

**Ответ:** Подтверди, что можно переходить к Phase 3/4 и генерировать `.dev/steps/*.md` на основе текущих ответов.
