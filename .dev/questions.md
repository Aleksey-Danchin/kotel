# Q&A Round 1

## Q1 — Пункт 1: Поведение `scripts/prisma-migrate.sh` и lifecycle `studio`

Сейчас в проекте есть только `scripts/dev-start.sh` и `scripts/dev-stop.sh`, а отдельного `scripts/prisma-migrate.sh` нет. В `infra/compose/dev.yml` сервис `studio` запускается командой `npx prisma studio --browser none --port 5555`. Нужно уточнить, как именно миграционный скрипт должен работать с контейнером `studio`: использовать уже запущенный, либо поднимать временно и завершать.

**Варианты:**
- a) Если `studio` уже запущен — использовать его; если не запущен — поднять `studio`, выполнить миграцию и остановить его.
- b) Всегда использовать только уже запущенный `studio`; если не запущен — завершать скрипт ошибкой.
- c) Всегда поднимать отдельный временный контейнер для миграции (независимо от статуса `studio`).
- d) Другое: ___

**Ответ:** Если `studio` уже запущен — использовать его и после перезапусть; если не запущен — поднять `studio`, выполнить миграцию и остановить его.

---

## Q2 — Пункт 1: Какая именно команда Prisma для миграции

В примере указан вызов `./scripts/prisma-migrate.sh create_user_table`, но не уточнено, что внутри: `prisma migrate dev --name`, `prisma migrate deploy` или другой сценарий. Для создания новой миграции обычно нужен `migrate dev --name <name>`.

**Варианты:**
- a) Использовать `npx prisma migrate dev --name <arg>`.
- b) Использовать `npx prisma migrate deploy` (без создания новой миграции).
- c) Поддерживать оба режима (например, флагом), уточните формат CLI.
- d) Другое: ___

**Ответ:** Использовать `npx prisma migrate dev --name <arg>`.

---

## Q3 — Пункт 1: Правила запуска и валидации аргумента имени миграции

Нужно зафиксировать UX скрипта: обязательность аргумента, допустимые символы имени миграции и директория запуска. Иначе поведение может быть неочевидным.

**Варианты:**
- a) Скрипт требует ровно 1 аргумент; если пусто/лишние аргументы — ошибка с usage.
- b) Допускать запуск без аргумента (генерировать имя автоматически).
- c) Разрешить доп. аргументы и прокидывать в Prisma.
- d) Другое: ___

**Ответ:** Скрипт требует ровно 1 аргумент; если пусто/лишние аргументы — ошибка с usage.

---

## Q4 — Пункт 2: Что значит «настроить studio на проведение всех имеющихся миграций»

Сейчас сервис `studio` в `infra/compose/dev.yml` запускает только Prisma Studio UI, без `migrate deploy`. Нужно понять целевое поведение: миграции должны применяться автоматически при старте `studio` или это отдельная ручная команда/скрипт.

**Варианты:**
- a) При каждом старте `studio` сначала выполнять `prisma migrate deploy`, потом запускать Studio.
- b) Не менять `studio`; сделать отдельный ручной скрипт для применения всех миграций через контейнер `studio`.
- c) Применять миграции только в `prisma-migrate.sh`, без изменения команды `studio`.
- d) Другое: ___

**Ответ:** При каждом старте `studio` сначала выполнять `prisma migrate deploy`, потом запускать Studio.

---

## Q5 — Пункт 3: Проверка/гарантия генерации `apps/prisma/client`

В `apps/prisma/prisma/schema.prisma` генератор клиента указывает `output = "../client"`. Нужно зафиксировать, это требование «проверить и задокументировать», или обязательно «форсировать генерацию» (например `prisma generate`) после миграции.

**Варианты:**
- a) Только проверить, что `migrate dev` уже генерирует клиент; отдельную команду не добавлять.
- b) Всегда после миграции явно выполнять `npx prisma generate`.
- c) Делать `generate` только если папка `apps/prisma/client` отсутствует.
- d) Другое: ___

**Ответ:** Всегда после миграции явно выполнять `npx prisma generate`.

---

## Q6 — Пункт 5: Какие права/владельца и на какой путь переопределять

Требование про `chown` сформулировано как пример `chown -R $USER:$USER ./prisma`, но в текущей структуре Prisma-файлы находятся в `apps/prisma/` (в т.ч. `apps/prisma/prisma`). Нужно зафиксировать точный путь и момент выполнения.

**Варианты:**
- a) `chown -R <uid:gid текущего пользователя> /app/apps/prisma` после каждого запуска скрипта (успех/ошибка).
- b) `chown` только для `/app/apps/prisma/prisma`.
- c) `chown` только при ошибке миграции.
- d) Другое: ___

**Ответ:** `chown -R <uid:gid текущего пользователя> /app/apps/prisma` после каждого запуска скрипта (успех/ошибка).

---

## Q7 — Пункт 6: Где размещать Prisma client factory с adapter-pg

Сейчас в backend нет Prisma-интеграции, а в `apps/prisma` есть только schema/types/config. Вы попросили: instance с adapter pg находится в prisma-директории, backend использует его как dependency. Нужно уточнить границу модулей и импорт.

**Варианты:**
- a) Создать factory в `apps/prisma` и импортировать его в backend через относительный импорт/путь workspace.
- b) Создать factory внутри backend (например `apps/backend/src/prisma`) и считать пункт про prisma-директорию концептуальным.
- c) Создать shared package/алиас для `apps/prisma` и подключить в backend через tsconfig paths.
- d) Другое: ___

**Ответ:** Создать factory в `apps/prisma` и импортировать его в backend через относительный импорт/путь workspace. В будущем возможно что нужно будет генерировать prisma instance для нескольких исполнителей. Так что нужно сразу позаботится о том, чтобы вынести factory в prisma.

---

## Q8 — Пункт 6: Контракт NestJS Prisma-модуля

В `apps/backend/src/app.module.ts` сейчас пустой `imports`, Prisma-модуля нет. Нужно определить минимальный контракт: какие провайдеры/экспорты ожидаются для следующих модулей.

**Варианты:**
- a) `PrismaModule` экспортирует `PrismaService` (обертка над готовым instance) для DI по классу.
- b) `PrismaModule` экспортирует токен (например `PRISMA_CLIENT`) + optional `PrismaService`.
- c) Только `PrismaService` без явного токена и без lifecycle hooks.
- d) Другое: ___

**Ответ:** `PrismaModule` экспортирует `PrismaService` (обертка над готовым instance) для DI по классу.

---

## Q9 — Пункт 7 + 10: Правила seed для `User` и хеширование пароля

В `apps/prisma/prisma/User.prisma` есть поля `fullname`, `login`, `passwordHash`. Вы отдельно указали установить `bcryptjs` в backend. Нужно уточнить: при seed пароли должны храниться как hash от `123` (предпочтительно), или буквально строкой `123` в `passwordHash`.

**Варианты:**
- a) Всегда сохранять bcrypt hash от `123` (с фиксированной cost).
- b) В seed временно писать `passwordHash: "123"` (для простоты теста).
- c) Хешировать, но алгоритм/параметры выбрать автоматически.
- d) Другое: ___

**Ответ:** Всегда сохранять bcrypt hash от `123` (с фиксированной cost). В целом можно даже не генрировать для всех hash, а общий для всех.

---

## Q10 — Пункт 7: Формат 100 пользователей

Нужно зафиксировать точный шаблон данных, чтобы step был однозначным.

**Варианты:**
- a) `fullname: "User <n>"`, `login: "user<n>"` для n=1..100.
- b) `fullname` и `login` оба строго `<n>` (как число/строка номера).
- c) Другой явный шаблон именования.
- d) Другое: ___

**Ответ:** `fullname: "User <n>"`, `login: "user<n>"` для n=1..100.

---

## Q11 — Пункт 8: Поведение скрипта seed и очистка БД

Вы хотите ручной seed через `studio` и полную очистку таблиц до сида. Нужно уточнить границы очистки и запуск миграций.

**Варианты:**
- a) Перед seed делать `prisma migrate reset --force --skip-generate` и затем запускать seed.
- b) Делать кастомный SQL `TRUNCATE ... CASCADE` для всех таблиц, затем seed.
- c) Очищать только таблицы приложения (сейчас только `User`), не трогая служебные таблицы миграций Prisma.
- d) Другое: ___

**Ответ:** Перед seed делать `prisma migrate reset --force --skip-generate` и затем запускать seed. Только не уверен на счет `--skip-generate`.

---

## Q12 — Пункт 9: Контракт `GET /api/users` и безопасность полей

Требуется полный путь frontend->backend->prisma и вывод в таблицу. Также в п.11 сказано «в Prisma base omit нужно прокинуть `passwordHash` и `login`». Нужно уточнить, какие поля API должен возвращать пользователю.

**Варианты:**
- a) Возвращать только `id`, `fullname`, `createdAt`, `updatedAt` (без `login`, `passwordHash`).
- b) Возвращать `id`, `fullname`, `login` (без `passwordHash`).
- c) Возвращать все поля кроме `passwordHash`.
- d) Другое: ___

**Ответ:** Возвращать все поля, кроме `login`, `passwordHash`. НО это нужно делать не в момент запроса. Для этого есть Prisma omit настройка при создание prisma instance.

---

## Q13 — Пункт 9: Поведение frontend страницы `/users`

Сейчас в frontend есть только базовые роуты (`~index`, `~$key`). Нужно уточнить UX страницы users и момент загрузки.

**Варианты:**
- a) Открытие `/users` показывает пустую таблицу + кнопку «загрузить»; данные грузятся только по клику.
- b) Данные грузятся автоматически при открытии, кнопка делает рефреш.
- c) Оставить только кнопку без таблицы до первой загрузки.
- d) Другое: ___

**Ответ:** Открытие `/users` показывает пустую таблицу + кнопку «загрузить»; данные грузятся только по клику.

---

## Q14 — Пункт 9: Ограничения по объему и пагинации users API

В seed будет 100 записей. Нужна ясность по пагинации/лимиту для тестового сценария.

**Варианты:**
- a) Без пагинации: вернуть все 100 пользователей одним ответом.
- b) Сделать примитивную пагинацию (`page`, `limit`) и в UI грузить первую страницу.
- c) Поддержать лимит на backend, но UI пока запрашивает все.
- d) Другое: ___

**Ответ:** Без пагинации: вернуть все 100 пользователей одним ответом. Сейчас задача проверить связность и доступность частей приложения.

---

## Q15 — Пункт 11: Что именно означает «Prisma base omit»

Сейчас в коде нет готового Prisma client instance, где можно централизованно задать omit. Нужно уточнить ожидаемую реализацию: глобальный omit в конструкторе Prisma Client или явный `select/omit` в каждом запросе.

**Варианты:**
- a) Глобальный base omit на уровне создания Prisma Client instance.
- b) Не глобально: в каждом запросе users вручную исключать поля.
- c) Глобально скрывать только `passwordHash`, а `login` скрывать выборочно.
- d) Другое: ___

**Ответ:** Глобальный base omit на уровне создания Prisma Client instance.

---

# Q&A Round 2

## Q16 — Уточнение к Q1: Что делать с уже запущенным `studio` после миграции

В ответе Q1 указано: «Если `studio` уже запущен — использовать его и после перезапусть». Нужно зафиксировать точное поведение после завершения `prisma-migrate.sh`, чтобы шаги были однозначными.

**Варианты:**
- a) Если `studio` уже был запущен до старта скрипта — оставить его запущенным (без рестарта/остановки).
- b) Если `studio` уже был запущен — после миграции обязательно перезапустить его.
- c) Если `studio` уже был запущен — после миграции остановить его.
- d) Другое: ___

**Ответ:** Если `studio` уже был запущен — после миграции обязательно перезапустить его. Это связано с тем, что prisma studio перестает работать, если была проведена миграция, а сама студия не была перезапущена. Особенность работы prisma studio util.

---

## Q17 — Уточнение к Q11: использовать ли `--skip-generate` при `migrate reset`

В Q11 выбран `prisma migrate reset --force --skip-generate`, но отмечено сомнение про `--skip-generate`. При этом в Q5 выбрано «всегда явно выполнять `prisma generate` после миграции». Для seed-скрипта нужно единое правило.

**Варианты:**
- a) Для seed: `migrate reset --force --skip-generate`, затем явный `prisma generate`, затем seed.
- b) Для seed: `migrate reset --force` (без `--skip-generate`), затем seed.
- c) Для seed: `migrate reset --force --skip-generate`, без отдельного `prisma generate`.
- d) Другое: ___

**Ответ:** Для seed: `migrate reset --force --skip-generate`, затем явный `prisma generate`, затем seed.

---

## Q18 — Пункт 8: Имя нового скрипта для ручного запуска seed

В требованиях сказано «в scripts добавить скрипт запуска seed через studio», но имя не зафиксировано. Нужно единое имя для step-файлов.

**Варианты:**
- a) `scripts/prisma-seed.sh`
- b) `scripts/prisma-seed-users.sh`
- c) `scripts/seed.sh`
- d) Другое: ___

**Ответ:** `scripts/prisma-seed.sh`. В будущем seed будет не только пользователей.
