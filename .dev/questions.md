# Q&A Round 1

## Q1 — Пункт 1: Условие показа шестеренки и тестовый сервер

В `apps/designer/src/state/headerGear.ts` сейчас проверка строгая: `new URL(serverUrl).host === window.location.host`.  
При запуске UI на `http://localhost:5173` и дефолтных серверах из `apps/designer/src/state/mocks.json` (`https://kotel.localhost`, `https://kotel-eu.localhost`, ...) условие почти всегда `false`, даже при роли `root/admin`.  
В предложении есть идея “дать одному из серверов подходящий localhost:5173”, но можно также менять саму логику сравнения.

**Варианты:**
- a) Оставить текущую строгую логику сравнения `host`, но изменить один дефолтный `serverUrl` на `http://localhost:5173` (или `https://localhost:5173`).
- b) Изменить правило сравнения (например, сравнивать только `hostname`, игнорируя порт/схему), дефолтные `serverUrl` не трогать.
- c) Добавить спец-правило для dev (`localhost`/`127.0.0.1` считаем совпадением с выбранным mock-сервером по отдельной логике).
- d) Другое: ___

**Ответ:** Оставить текущую строгую логику сравнения `host`, но изменить один дефолтный `serverUrl` на `http://localhost:5173` (или `https://localhost:5173`).

---

## Q2 — Пункт 2: Скелетон при переключении сервера и на старте страницы

Сейчас в `apps/designer/src/routes/~$id/~index.tsx` есть только `streamLoading` для смены чата/сервера в контенте сообщений (`ChatMessageBodySkeleton`), но нет отдельного состояния “загрузка серверов” в `ServicesColumn`.  
Для реализации нужно зафиксировать UX: где именно должен появляться скелетон и что он заменяет в момент загрузки.

**Варианты:**
- a) На старте показываем скелетон только в колонке серверов; при переключении сервера — только в области сообщений.
- b) На старте показываем скелетон во всех 3 колонках; при переключении сервера — скелетон в чатах + сообщениях.
- c) Один общий fullscreen/overlap скелетон на старте и при server-switch.
- d) Другое: ___

**Ответ:** На старте показываем скелетон во всех 3 колонках; при переключении сервера — скелетон в чатах + сообщениях.

---

## Q3 — Пункт 2: Длительность и триггеры скелетона

Для чата сейчас используется фиксированная задержка `CHAT_STREAM_LOAD_MS = 420`.  
Нужно понять, должны ли задержки быть одинаковыми для startup/server-switch/chat-switch, и должен ли server-loading срабатывать при каждом выборе сервера или только при первом входе.

**Варианты:**
- a) Одна фиксированная задержка для всех кейсов (например, 400–500мс).
- b) Разные задержки: startup дольше, переключения короче.
- c) Без искусственной задержки (показывать скелетон только на реальное время вычисления/рендера).
- d) Другое: ___

**Ответ:** Одна фиксированная задержка для всех кейсов (1200мс).

---

## Q4 — Пункт 3: Что значит “main должен быть просто пустым”

Сейчас при отсутствии выбранного чата route-компонент возвращает `null`, но сама колонка (`ChatColumn`) всё равно рендерит шапку (`"Выберите чат"`) и футер-композер (disabled).  
Уточни, что считать “просто пустым”: только body или вся правая колонка без header/footer.

**Варианты:**
- a) Пустой только body (header/footer остаются).
- b) Полностью пустая правая колонка: без header, без footer, без placeholder-текста.
- c) Header оставить, footer скрывать, body пустой.
- d) Другое: ___

**Ответ:** Полностью пустая правая колонка: без header, без footer, без placeholder-текста.

---

## Q5 — Пункт 4: Прижатие чата к низу при короткой истории

В `ChatMessageList` список сообщений — обычный `flex flex-col`, поэтому при малом количестве сообщений контент начинается сверху.  
Чтобы “прижать к низу”, нужно уточнить поведение для разных случаев (короткий/длинный чат и после отправки).

**Варианты:**
- a) Прижимать к низу только когда сообщений мало (контент ниже высоты viewport body).
- b) Всегда визуально тянуть список вниз (включая длинные истории, с корректным scroll).
- c) Прижимать к низу только для личных чатов (`type=person`), для group оставить как сейчас.
- d) Другое: ___

**Ответ:** Всегда визуально тянуть список вниз (включая длинные истории, с корректным scroll).

---

## Q6 — Пункт 5: Новый чат без сообщений

В `apps/designer/src/state/mocks.json` все текущие чаты связаны с сообщениями через `chatMessages`.  
Для “чата без сообщений” нужно определить, где он должен появляться и как называться, чтобы acceptance был проверяемым.

**Варианты:**
- a) Добавить 1 новый чат в `srv_main`.
- b) Добавить по одному пустому чату в каждый сервер.
- c) Добавить пустой чат только в `srv_local` (для тестов).
- d) Другое: ___

**Ответ:** Добавить 1 новый чат в `srv_main`. И добавь сервер без чатов. Когда нет чатов нужна плашка в колонке чатов "Чатов нет". При пустом количестве сообщений в чате, но при существующим чате, планшка "Сообщений нет".

---

## Q7 — Пункт 6: Единая высота header у всех колонок

Сейчас высоты шапок разные из-за разных paddings/типографики:  
- `ServicesColumn` header: `px-2 py-2` без border,  
- `ChatsColumn` header: `border-b ... px-2 py-2`,  
- `ChatColumn` header: `px-4 py-3`.  
Нужно зафиксировать целевую высоту (или единый набор классов), включая место под gear-кнопку.

**Варианты:**
- a) Ввести фиксированную min-height (например, `min-h-12`) и единые paddings у всех 3 header.
- b) Вынести общий компонент/класс для header и использовать его везде.
- c) Оставить разные paddings, но выровнять только итоговую высоту через utility-класс.
- d) Другое: ___

**Ответ:**  Ввести фиксированную min-height (например, `min-h-12`) и единые paddings у всех 3 header.

---

## Q8 — Пункт 7: Фон колонок (контраст между колонками)

Сейчас колонки используют `bg-base-300`, `bg-base-200`, `bg-base-100`, но визуально на выбранной теме это может сливаться.  
Нужно уточнить ожидаемый уровень контраста и принцип (фиксированные токены, opacity, бордеры + фон).

**Варианты:**
- a) Усилить различие только сменой bg-токенов DaisyUI.
- b) Оставить текущие bg, но усилить разделение бордерами/тенями.
- c) Комбинация: и bg, и бордеры/внутренние тени.
- d) Другое: ___

**Ответ:** Усилить различие только сменой bg-токенов DaisyUI.

---

## Q9 — Пункт 8: Правый бордер третьей колонки

В `ChatColumn` сейчас есть `border-l-2 ...`, но нет `border-r`, поэтому правый край может теряться визуально.  
Нужно зафиксировать, где именно должен быть бордер (внутри колонки/внешний контейнер) и какой толщины.

**Варианты:**
- a) Добавить `border-r` на корневой контейнер `ChatColumn`.
- b) Добавить правый бордер на контейнер в `~__root.tsx` (обёртка третьей колонки).
- c) Добавить и левый, и правый бордер одинаковой толщины/цвета на `ChatColumn`.
- d) Другое: ___

**Ответ:** Добавить правый бордер на контейнер в `~__root.tsx` (обёртка третьей колонки).

---

## Q10 — Пункт 9: Автофокус формы чата

В `ChatColumn` textarea сейчас не фокусируется автоматически при выборе/открытии чата.  
Нужно уточнить, когда именно должен ставиться фокус: только при входе в чат или при каждом переключении сервера/чата, и нужен ли возврат фокуса после отправки.

**Варианты:**
- a) Фокус только при первом открытии конкретного чата.
- b) Фокус при каждом переключении на любой чат.
- c) Фокус при открытии + сохранять фокус после отправки Enter/кнопкой.
- d) Другое: ___

**Ответ:** Фокус при каждом переключении на любой чат. Фокус при открытии + сохранять фокус после отправки Enter/кнопкой.

---

## Q11 — Пункт 10: Sticky-автоскролл — критерий “прижато к низу”

Для режима sticky нужно формально определить условие “последнее сообщение прижато к низу”.  
Технически это обычно `scrollHeight - (scrollTop + clientHeight) <= threshold`.

**Варианты:**
- a) Threshold 0px (строгое совпадение).
- b) Threshold 8–16px (типичный UX-допуск).
- c) Threshold 24–32px (более мягкое условие).
- d) Другое: ___

**Ответ:** Threshold 24–32px (более мягкое условие).

---

## Q12 — Пункт 10: На какие новые сообщения распространяется sticky

Сейчас новые сообщения появляются как:
- исходящее сообщение сразу после send,
- входящее авто-сообщение через ~2с (`sendDesignerChatMessage`).
Нужно уточнить, автоскролл в sticky должен срабатывать на оба типа или только на входящие.

**Варианты:**
- a) Автоскролл на любые новые сообщения (исходящие и входящие).
- b) Только на входящие авто-ответы.
- c) Только на исходящие пользователя.
- d) Другое: ___

**Ответ:** Автоскролл на любые новые сообщения (исходящие и входящие).

---

## Q13 — Пункт 10: Восстановление sticky после ручной прокрутки

Ты описал: если пользователь ушёл в историю, sticky выключается.  
Нужно уточнить, как sticky включается обратно:
- только при ручном скролле обратно в низ,
- или также при отправке пользователем нового сообщения.

**Варианты:**
- a) Когда пользователь вручную доскроллит до низа.
- b) При отправке своего сообщения sticky автоматически включается снова.
- c) Гибрид: для Enter-send включаем, для автоответа — нет.
- d) Другое: ___

**Ответ:** a + b

---

## Q14 — Пункты 1–10: Что делать с уже выполненными частями из archive/006

В `suggestion.md` указано, что шаги из `./dev/archive/006-front-columns/questions.md` “выполнены”, но есть замечания/дополнения.  
Чтобы шаги были корректно декомпозированы, нужно зафиксировать стратегию: правим существующую реализацию только точечно по замечаниям или допускается частичный рефактор, если это упростит sticky-scroll/skeleton/focus.

**Варианты:**
- a) Только минимальные правки по 10 пунктам, без больших рефакторов.
- b) Разрешён умеренный рефактор (структура компонентов/атомов), если улучшает поддержку.
- c) Разрешён полный рефактор в рамках `apps/designer`, если критерии сохраняются.
- d) Другое: ___

**Ответ:** Разрешён полный рефактор в рамках `apps/designer`, если критерии сохраняются.

---

Дополнение
1) Если режим sticky то над кнопкой "отправить" должная появисят икнока кнопка со стрелочкой вниз для автоматической прокрутки в самый них. Назовем эту кнопку "вниз чата".
2) Если видна "вниз чата" и пришли новый сообщения, то вместе с иконкой (а точнее над ней) число новых сообщений и цвет border изменен на primary.

# Q&A Round 2

## Q15 — Дополнение: Включать ли новые пункты про кнопку "вниз чата" в текущий compute-steps

Я вижу внизу `questions.md` новый блок “Дополнение” с двумя новыми требованиями (кнопка “вниз чата” + счётчик новых сообщений).  
Эти требования не были в исходном `suggestion.md`, поэтому по процессу нужно явно подтвердить: включаем их в текущий набор шагов или переносим в следующий suggestion.

**Варианты:**
- a) Включить оба новых требования в текущий compute-steps.
- b) Не включать сейчас, оставить только исходные 10 пунктов из `suggestion.md`.
- c) Включить только кнопку “вниз чата”, а счётчик отложить.
- d) Другое: ___

**Ответ:** Включить оба новых требования в текущий compute-steps.

---

## Q16 — Уточнение к дополнению: когда должна показываться кнопка "вниз чата"

В тексте дополнения есть потенциальное противоречие:  
- п.1 говорит “Если режим sticky — показывать кнопку вниз”,  
- п.2 говорит “Если кнопка видна и пришли новые сообщения, показывать счётчик”, что обычно соответствует **не-sticky** режиму (когда пользователь ушёл вверх).  
Нужно зафиксировать точное правило видимости кнопки.

**Варианты:**
- a) Кнопка видна, когда sticky **выключен** (пользователь не у низа), чтобы быстро вернуться вниз.
- b) Кнопка видна, когда sticky **включен**.
- c) Кнопка видна всегда при открытом чате.
- d) Другое: ___

**Ответ:**  Кнопка видна, когда sticky **выключен** (пользователь не у низа), чтобы быстро вернуться вниз.

---

## Q17 — Уточнение к дополнению: поведение счётчика новых сообщений

Для реализации счётчика над кнопкой “вниз чата” нужно зафиксировать деталь UX:
- какие сообщения считать “новыми” (входящие/исходящие/оба),
- когда счётчик сбрасывается,
- как именно меняется border (какого элемента).

**Варианты:**
- a) Считать только входящие; сброс при клике “вниз чата” или при ручном доскролле до низа; border меняется у самой кнопки.
- b) Считать все новые (входящие+исходящие); сброс только по клику на кнопку; border у кнопки.
- c) Считать только входящие; сброс при любом возврате sticky; border меняется у области footer.
- d) Другое: ___

**Ответ:** Считать только входящие; сброс при клике “вниз чата” или при ручном доскролле до низа; border меняется у самой кнопки.