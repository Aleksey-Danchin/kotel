# Q&A Round 1

## Q1 — Пункт 1: модель пользователей в моках

Сейчас в `apps/designer/src/state/mocks.json` есть `servers`, `chats`, `messages`, `serverChats`, `chatMessages`, но нет отдельного каталога `users` и связки `serverUsers`. В коде `apps/designer/src/state/store.ts` тоже нет типов/селекторов для пользователей сервера. Чтобы корректно спроектировать шаги, нужно зафиксировать целевую структуру мок-данных.

**Варианты:**
- a) Добавить глобальный массив `users` и связи `serverUsers: [serverId, userId][]` (как в тексте).
- b) Добавить только `serverUsers`, а данные пользователя хранить внутри `servers`.
- c) Для каждого сервера хранить вложенный список пользователей прямо в объекте сервера.
- d) Другое: ___

**Ответ:** Добавить глобальный массив `users` и связи `serverUsers: [serverId, userId][]` (как в тексте).

---

## Q2 — Пункты 2 + 15 + 16: кто считается "текущим пользователем" сервера

В моках `servers[].user` уже хранит текущую сессию по серверу, а пункт 2 просит "на каждом сервере должен быть пользователь, который авторизован на этом сервере". Нужно уточнить, это просто подтверждение текущей модели или требуется новая явная связь (например, `server.currentUserId`) и синхронизация с таблицей пользователей.

**Варианты:**
- a) Оставляем `servers[].user` как источник текущей сессии, дополнительно связываем его с `users`.
- b) Вводим отдельное поле `currentUserId` на сервере, `servers[].user` убираем/не используем.
- c) Оба варианта храним параллельно (с обязательной консистентностью).
- d) Другое: ___

**Ответ:** Оставляем `servers[].user` как источник текущей сессии, дополнительно связываем его с `users`.

---

## Q3 — Пункт 3: id пользователей в сообщениях

Сейчас у сообщений есть поле `author` (`apps/designer/src/state/store.ts`, интерфейс `ChatMessage`), и уже используются значения вроде `mock-user-1`, `peer-maria`. Нужно уточнить, требование "сообщениям дать id пользователей" означает переименование поля или изменение данных.

**Варианты:**
- a) Ничего не переименовывать: `author` уже трактуем как `userId`.
- b) Переименовать `author` -> `authorUserId` во всех моках и коде.
- c) Добавить новое поле `authorUserId`, `author` оставить для обратной совместимости.
- d) Другое: ___

**Ответ:** Переименовать `author` -> `userId` во всех моках и коде.

---

## Q4 — Пункты 4 + 6: блок "Пользователи" в колонке чатов и открытие диалога

Сейчас `apps/designer/src/routes/ChatsColumn.tsx` рендерит только список чатов. Пункт 4 требует под ним отдельный список пользователей, а пункт 6 — клик по пользователю открывает/создаёт personal-чат. Нужны правила, как именно связывать пользователя и чат.

**Варианты:**
- a) Для пользователя есть детерминированный `person` чат id (например, `dm_<userId>`), при отсутствии создаём в сторе.
- b) Клик всегда создаёт новый чат (даже если уже был), старый не переиспользуем.
- c) Если чат уже есть — открываем его, иначе создаём один раз и потом переиспользуем.
- d) Другое: ___

**Ответ:** Если чат уже есть — открываем его, иначе создаём один раз и потом переиспользуем.

---

## Q5 — Пункт 4: состав и сортировка списка пользователей под чатами

Нужно уточнить, каких именно пользователей показывать под чатами в `ChatsColumn`: всех пользователей текущего сервера, только "кроме себя", с какой сортировкой, и нужны ли роли/логины в карточке.

**Варианты:**
- a) Показывать всех пользователей сервера, включая текущего.
- b) Показывать только пользователей сервера, кроме текущего.
- c) Показывать только тех, с кем уже есть personal-чат.
- d) Другое: ___

**Ответ:** Показывать только пользователей сервера, кроме текущего. В карточке пользователя пока только имя, маркер онлайн и дата последней детекции онлай

---

## Q6 — Пункт 5: поиск в шапке колонки чатов

В `ChatsColumn.tsx` сейчас в header только заголовок + `ColumnHeaderGear`. Поиск должен "сразу сортировать и чаты, и пользователей" с debounce 250мс и крестиком очистки. Нужно уточнить точное поведение сортировки.

**Варианты:**
- a) Поиск = фильтр: показываем только совпавшие элементы (чаты и пользователи).
- b) Поиск = сортировка: показываем все, но совпавшие поднимаем вверх.
- c) Гибрид: сначала совпавшие (отсортированные), затем остальные.
- d) Другое: ___

**Ответ:** Поиск = фильтр: показываем только совпавшие элементы (чаты и пользователи). Причем поиск не строгий, регистронезависимый и не зависит от очередности слов. Например на "ди ал" может быть найдне "Далин Ог`Дип".

---

## Q7 — Пункт 5: критерий матчинга для поиска

Для шага нужен чёткий алгоритм: искать только по заголовку (`title/fullname`) или также по `subtitle`, `login`, регистронезависимо ли, нужен ли fuzzy-match.

**Варианты:**
- a) Только `chat.title` и `user.fullname`, без учета регистра.
- b) `chat.title + chat.subtitle + user.fullname + user.login`, без учета регистра.
- c) Вариант b + fuzzy/частичное "приблизительное" совпадение.
- d) Другое: ___

**Ответ:** Только `chat.title` и `user.fullname`, без учета регистра.

---

## Q8 — Пункты 7 + 8 + 9: онлайн-маркеры и источник статуса

Сейчас в `ChatCard`, будущей карточке пользователя и header чата нет online-индикатора. Нужен единый источник "онлайн" по пользователю и правило "рандомно". Нужно решить, должен ли "рандом" быть стабильным между перерендерами.

**Варианты:**
- a) Стабильный псевдо-рандом от `userId` (одинаково при каждом рендере).
- b) Генерировать при старте приложения и хранить в состоянии.
- c) Полностью случайно на каждый рендер (может мигать).
- d) Другое: ___

**Ответ:** Стабильный псевдо-рандом от `userId` (одинаково при каждом рендере).

---

## Q9 — Пункт 7: где показывать маркер онлайн в карточке чата

В `apps/designer/src/components/ChatCard.tsx` карточка состоит из title/subtitle/unread badge. Нужно зафиксировать точное место зелёной точки для personal-чата.

**Варианты:**
- a) Рядом с именем чата в первой строке.
- b) В правой части строки рядом с unread badge.
- c) Перед подзаголовком отдельной строкой.
- d) Другое: ___

**Ответ:** Рядом с именем чата в первой строке.

---

## Q10 — Пункт 10: `background-base-300` для header

Сейчас у трёх колонок разные `bg`/границы (`ServicesColumn`, `ChatsColumn`, `ChatColumn`). Нужно подтвердить охват требования: только header трёх колонок или также шапки в модалках/страницах настроек.

**Варианты:**
- a) Только header трёх основных колонок (Сервера/Чаты/Текущий чат).
- b) Все header во всём дизайнере, включая настройки и модалки.
- c) Только то, что относится к чату (3 колонки + настройки внутри чата).
- d) Другое: ___

**Ответ:**  Только header трёх основных колонок (Сервера/Чаты/Текущий чат).

---

## Q11 — Пункты 11 + 12: общий контейнер режима настроек и переключение табов

Сейчас `ColumnHeaderGear` в `apps/designer/src/components/ColumnHeaderGear.tsx` только рисует кнопку и не открывает UI. Нужно уточнить, где и как рендерить full-screen settings (вместо колонок или оверлей поверх них), и как трактовать "занимая весь главный контейнер".

**Варианты:**
- a) Полностью заменяем содержимое правого `main` (где `ChatColumn`) на settings-экран.
- b) Показываем модальный оверлей поверх всех 3 колонок внутри `max-w-[1400px]`.
- c) Делаем отдельный route/страницу настроек, переход по шестерёнке.
- d) Другое: ___

**Ответ:** Показываем модальный оверлей поверх всех 3 колонок. Пусть размер главного container и отцентровка совпадают с основными.

---

## Q12 — Пункт 27: какая шестерёнка должна открывать какой таб

Есть три шестерёнки (в `ServicesColumn`, `ChatsColumn`, `ChatColumn`). Пункт 27 говорит "открывается сразу таб в зависимости от того какая именно шестеренка была нажата". Нужно сопоставление.

**Варианты:**
- a) Сервера -> "основной", Чаты -> "конфигуратор", Чат -> "пользователи".
- b) Любая шестерёнка для ADMIN/ROOT открывает последний активный таб, для USER — только "аккаунт".
- c) Сервера -> "основной", Чаты -> "пользователи", Чат -> "аккаунт".
- d) Другое: ___

**Ответ:** Сервера -> "основной", Чаты -> "конфигуратор", Чат -> "пользователи". Для USER — только "аккаунт". Для USER другие табы не доступны.

---

## Q13 — Пункты 11 + 17: закрытие настроек (крестик + Esc)

Для чата Esc уже используется в `~__root.tsx` как навигация "чат -> сервер -> root". Нужно уточнить приоритет в режиме настроек: закрывать настройки по Esc и блокировать текущую цепочку выхода, чтобы избежать конфликтов.

**Варианты:**
- a) В режиме настроек Esc всегда закрывает настройки, навигация чата по Esc временно отключается.
- b) Esc сначала срабатывает как текущая навигация, настройки закрываются только крестиком.
- c) Esc внутри настроек работает только если фокус не в инпуте.
- d) Другое: ___

**Ответ:** В режиме настроек Esc всегда закрывает настройки, навигация чата по Esc временно отключается.

---

## Q14 — Пункт 13: поле "название сервера" (основной таб)

В `ServerCard` имя сейчас вычисляется как host из `serverUrl`. Пункт 13 просит на табе "основной" настраивать название сервера для карточки. Нужно решить, где хранить это имя и что делать с текущими данными.

**Варианты:**
- a) Добавить новое поле `serverDisplayName` в `ServerSession`, fallback = host.
- b) Переиспользовать `serverUrl` (редактировать его как "название" нельзя, только отображение).
- c) Хранить в отдельной карте `serverNamesById`.
- d) Другое: ___

**Ответ:** server.name: string. Новое поле в схеме сервера.

---

## Q15 — Пункт 14: "конфигуратор" (черновик)

Нужно уточнить уровень детализации "черновика": только disabled-поле "частота отправки запросов" и кнопка "сохранить", или ещё заголовки/описания/валидация фиктивного значения.

**Варианты:**
- a) Минимум: label + disabled input + кнопка "Сохранить".
- b) Добавить также helper-text о том, что это черновик.
- c) Добавить disabled input с фиксированным числом (например, 1000ms) и без сохранения.
- d) Другое: ___

**Ответ:** Минимум: label + disabled input + кнопка "Сохранить".

---

## Q16 — Пункт 15: страница "пользователи" — источник и объём данных

Сейчас в проекте есть статическая страница `/users` (`apps/designer/src/routes/~users/~index.tsx`), но она не связана с чатом и не содержит полей login/role/password/blocked. Нужно уточнить, реиспользуем ли её как базу для вкладки настроек или делаем отдельную UI-модель внутри chat settings.

**Варианты:**
- a) Не переиспользуем `/users`, делаем отдельную модель/компоненты в режиме настроек.
- b) Частично переиспользуем таблицу `/users` и расширяем её.
- c) Полностью мигрируем `/users` в новый settings-tab и route `/users` убираем.
- d) Другое: ___

**Ответ:** Не переиспользуем `/users`, делаем отдельную модель/компоненты в режиме настроек.

---

## Q17 — Пункт 15: правило immutable для ROOT

Фраза "ROOT иммутабельные" может означать разные уровни блокировки. Нужно точно определить, что запрещено менять у ROOT-пользователя.

**Варианты:**
- a) У ROOT нельзя менять вообще ничего (все поля read-only, без save).
- b) У ROOT нельзя менять только `role`, остальные поля можно.
- c) У ROOT нельзя менять `login`/`role`, но можно `fullname`/`password`/`blocked`.
- d) Другое: ___

**Ответ:**  У ROOT что-то менять может только ROOT. НО роль изменить у ROOT не может никто.

---

## Q18 — Пункт 16: блок "Аккаунт" и права ADMIN

Нужны точные последствия кнопки "Перестать быть админом" (если `user.role=ADMIN`) в песочнице: меняем роль в локальном сторе, нужна ли модалка подтверждения, куда девать пользователя после смены роли (ведь после этого доступ только к вкладке аккаунта по пункту 27).

**Варианты:**
- a) По клику сразу меняем роль ADMIN -> USER без подтверждения.
- b) Показываем confirm modal, после подтверждения меняем роль.
- c) Только UI-кнопка без изменения данных (черновик).
- d) Другое: ___

**Ответ:** Нужно модальное окно с подтверждением действия. Изменения конечно только локальные. designer это UI песочница.

---

## Q19 — Пункт 16: блок завершения сессий

Сейчас в моках нет модели сессий пользователя. Нужно определить минимальную структуру для "список активных сессий + TTL + удаление с confirm, без крестика у текущей".

**Варианты:**
- a) Добавляем мок-массив `sessions` (id, device/name, expiresAt, isCurrent).
- b) Генерируем сессии на лету в компоненте без состояния (чисто визуально).
- c) Храним в jotai состоянии, чтобы удаление реально меняло список.
- d) Другое: ___

**Ответ:** Добавляем мок-массив `sessions` (id, serverId). isCurrent определяется по текущему выбранному server.

---

## Q20 — Пункт 18: модалка "добавить пользователя" и генератор пароля

Нужно зафиксировать требования к генерации пароля: длина, допустимые символы, должен ли пароль сразу подставляться в input, можно ли редактировать после генерации.

**Варианты:**
- a) Генерировать 12 символов [A-Za-z0-9], подставлять в поле, можно редактировать.
- b) Генерировать 16 символов со спецсимволами, подставлять, можно редактировать.
- c) Оставить кнопку-заглушку без реальной генерации (черновик).
- d) Другое: ___

**Ответ:** Генерировать 12 символов [A-Za-z0-9], подставлять в поле, можно редактировать.

---

## Q21 — Пункт 20: "последний активный чат сервера"

В `store.ts` уже есть `lastChatByServerIdAtom` + `effectiveChatIdAtom`, а в `designerNavigation.ts` есть очистка lastChat при выходе из чата; в `routes/~$id/~index.tsx` lastChat обновляется при выборе чата. То есть значительная часть поведения уже реализована. Подтверди, считаем ли этот пункт закрытым или нужны дополнительные сценарии.

**Варианты:**
- a) Считаем пункт уже реализованным, в шаги не включаем.
- b) Нужны дополнительные фиксы (опишу ниже в свободной форме).
- c) Не уверен, оставляем в шагах как проверку/аудит с тестами.
- d) Другое: ___

**Ответ:** Сейчас работает не корректно. Если я был на serverA, переключился на serverB (без выхода из chatA на serverA), и переключился на serverA, то chatA автоматически не открывается. А должен, ведь я из него явно не выходил.

---

## Q22 — Пункты 21 + 25: ограничения textarea (1024), авто-рост 2..10 строк и счётчик

Сейчас в `ChatColumn.tsx` textarea `rows={2}`, `resize-y`, без лимита и без счётчика. Нужно уточнить UX: запрет ввода после 1024, поведение при вставке большого текста, позиция счётчика и формат.

**Варианты:**
- a) Жёсткий `maxLength=1024`, счётчик "осталось N" показывать с 50 и ниже.
- b) Не использовать `maxLength`, вручную обрезать лишнее в `onChange`.
- c) Счётчик показывать всегда, но красным только при 0.
- d) Другое: ___

**Ответ:** Жёсткий `maxLength=1024`, счётчик "осталось N" показывать с 50 и ниже.

---

## Q23 — Пункт 22: дневные бейджи между сообщениями

Сейчас `ChatMessageList.tsx` рендерит плоский список сообщений без разделителей дат. Нужен точный формат бейджа, локаль и позиционирование.

**Варианты:**
- a) daisyUI `badge` по центру: "26 мар", один на каждую дату.
- b) Разделитель-строка с датой (не badge), центрированный.
- c) Badge с полным форматом даты `dd.mm.yyyy`.
- d) Другое: ___

**Ответ:** daisyUI `badge` по центру: "26 мар", один на каждую дату имеющую сообщения.

---

## Q24 — Пункт 23: формат времени сообщения

В `ChatMessageList.tsx` сейчас формат всегда `day month hour:minute`. Нужно зафиксировать точный формат для 3 случаев: сегодня / этот год / другой год.

**Варианты:**
- a) Сегодня: `HH:mm`; этот год: `d MMM, HH:mm`; другой год: `d MMM yyyy, HH:mm`.
- b) Сегодня: `HH:mm`; этот год: `dd.MM HH:mm`; другой год: `dd.MM.yyyy HH:mm`.
- c) Сегодня: `HH:mm:ss`; остальное как сейчас.
- d) Другое: ___

**Ответ:** "23:12" "12 апр. 23:12" "2001 12 апр. 23:12". Следует использовать Intl.DateTimeFormat

---

## Q25 — Пункт 24: виртуализация списка сообщений

В `ChatMessageList.tsx` обычный `.map`, без виртуализации. Нужно уточнить допустимую библиотеку/подход для песочницы (`@tanstack/react-virtual` vs ручная), и критично ли поддержать sticky-логику из `ChatColumn` без регрессий.

**Варианты:**
- a) Использовать `@tanstack/react-virtual` и адаптировать sticky.
- b) Использовать другую библиотеку виртуализации (укажи какую).
- c) Сделать упрощённую windowing-реализацию вручную.
- d) Другое: ___

**Ответ:** Использовать `@tanstack/react-virtual` и адаптировать sticky. Устанавливать через npm

---

## Q26 — Пункт 26: критерий сохранения sticky-режима

Сейчас sticky теряется, когда `isNearBottom` (порог 28px) становится false в `ChatColumn.tsx`. Формулировка "если прокрутить побольше" требует численного порога/правила повторного включения sticky.

**Варианты:**
- a) Sticky выключается только если пользователь прокрутил вверх больше чем на N px (укажи N).
- b) Sticky выключается только после явного ручного скролла + паузы.
- c) Sticky выключается только при достижении верхней половины списка.
- d) Другое: ___

**Ответ:** Видимо сейчас sticky выключается из-за того, что появляется новое сообщение и растягивает область и срабатывает сброс из-за того, что сролл сначала увеличивается, а после этого просчитывается sticky. Это предположение. Давай на всякий случай в режиме sticky сначала прокручивать вниз скролл, потом добавлять сообщение, потом прокручивать еще раз.

---

## Q27 — Пункт 27: матрица доступа к табам настроек по ролям

Нужно точно зафиксировать права в settings-режиме. Сейчас `ColumnHeaderGear` показывает кнопку только для `admin|root` и только если host совпадает (`state/headerGear.ts`), но пункт 27 требует показывать шестерёнку всегда и ограничивать не саму кнопку, а набор табов.

**Варианты:**
- a) ROOT/ADMIN текущего сервера: 4 таба; остальные роли: только "Аккаунт".
- b) ROOT: 4 таба, ADMIN: 3 таба (без конфигуратора), USER: только "Аккаунт".
- c) Для "чужого" сервера всегда только "Аккаунт", даже для ADMIN/ROOT.
- d) Другое: ___

**Ответ:** ROOT/ADMIN текущего сервера: 4 таба; остальные роли: только "Аккаунт".

---

# Q&A Round 2

## Q28 — Уточнение к Q5: "дата последней детекции онлайна" в карточке пользователя

В ответе к Q5 добавлено требование показывать в карточке пользователя "дата последней детекции онлай". Сейчас в моках вообще нет поля для времени online/offline, поэтому нужно зафиксировать формат и источник этого значения, чтобы шаги были однозначными.

**Варианты:**
- a) Добавить в `users` поле `lastSeenAt` (ISO), в карточке форматировать как человеко-читаемую дату/время.
- b) Добавить поле `lastOnlineLabel` (готовая строка), без форматирования в UI.
- c) Для online-пользователя показывать "online", для offline — "был(а) в сети <время>" на основе псевдо-данных.
- d) Другое: ___

**Ответ:** Добавить в `users` поле `lastSeenAt` (ISO), в карточке форматировать как человеко-читаемую дату/время. И добавить "isOnline: boolean".

---

## Q29 — Уточнение к Q17: точная матрица редактирования ROOT

Ответ "У ROOT что-то менять может только ROOT. НО роль изменить у ROOT не может никто" требует уточнения, чей именно ROOT имеется в виду и что делать с self-edit в табе "Аккаунт".

**Варианты:**
- a) В вкладке "Пользователи": ADMIN не может редактировать ROOT вообще; ROOT может редактировать ROOT (кроме role). В "Аккаунт" ROOT может менять свой login/fullname/password.
- b) Любого ROOT нельзя редактировать вообще ни при каких условиях (включая самого себя).
- c) ROOT можно редактировать только в "Аккаунт", но не в "Пользователи".
- d) Другое: ___

**Ответ:** В вкладке "Пользователи": ADMIN не может редактировать ROOT вообще; ROOT может редактировать ROOT (кроме role). В "Аккаунт" ROOT может менять свой login/fullname/password.

---

## Q30 — Уточнение к Q19: "время жизни" и действия по сессиям

В исходном пункте 16 требуются список активных сессий, время жизни и 3 действия завершения сессий. В ответе задана только структура `(id, serverId)` и правило `isCurrent`. Нужны недостающие детали отображения TTL и поведения трёх кнопок.

**Варианты:**
- a) Расширить мок сессии до `{id, serverId, expiresAt}`; TTL считать как `expiresAt - now` и показывать текстом; 3 кнопки реально фильтруют/удаляют сессии в локальном состоянии.
- b) Оставить `{id, serverId}` и показывать фиктивный статический TTL-текст без вычисления; кнопки только визуальные.
- c) Добавить `{id, serverId, ttlMinutes}` и показывать как "NN мин"; кнопки меняют локальное состояние.
- d) Другое: ___

**Ответ:** Расширить мок сессии до `{id, serverId, createdAt}` Нужно время сколько эта сессия уже существует.

---

## Q31 — Уточнение к Q24: подтверждение формата "год первым"

Вы указали примеры: `"23:12"`, `"12 апр. 23:12"`, `"2001 12 апр. 23:12"` с `Intl.DateTimeFormat`. Подтверждаю, что для сообщений не текущего года нужно именно форматировать как `YYYY D MMM HH:mm` (год в начале), а не `D MMM YYYY HH:mm`.

**Варианты:**
- a) Да, именно `YYYY D MMM HH:mm` (год первым).
- b) Нет, нужен обычный порядок `D MMM YYYY HH:mm`.
- c) Нужен другой формат: ___
- d) Другое: ___

**Ответ:** Вообще пологаюсь на DateTimeFormat, вот как он выводит, та и будет.

---

## Q32 — Уточнение к Q26: sticky и порядок операций отправки

Вы предложили порядок "в режиме sticky сначала прокручивать вниз, потом добавлять сообщение, потом прокручивать еще раз". Нужно зафиксировать, применяем ли это только к локальной отправке/автоответу или ко всем новым сообщениям (включая initial load и смену чата/сервера).

**Варианты:**
- a) Применять ко всем сценариям появления новых сообщений, когда sticky=true.
- b) Только при отправке и автоответе в текущем чате.
- c) Только при входящих сообщениях, исходящие оставить как есть.
- d) Другое: ___

**Ответ:** Применять ко всем сценариям появления новых сообщений, когда sticky=true.

---

## Q33 — Финальное подтверждение перед генерацией шагов

I've gathered all the details I need and I'm ready to generate the implementation steps. Is there anything else I should know — any remaining details, concerns, or context?

**Ответ:** 

---