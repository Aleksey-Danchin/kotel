1) Добавил в проект ./apps/mobile expo проект. Это будет часть проекта, которая реализует клиент для android и ios. Нужно включить ее в старт в dev режим. Для нее отдельный контейнер нужен так же, но этот контейнер должен быть остаться активным в терминале. Там будут элементы управления и qr код для входа через специализированное приложение.

2) Нужно в нем продублировать страницы users и session-test из frontend. Стили повторя не нужно, только функционал для тестирования обращения к backend.

3) Нужна твой оценка как его тестировать. Точно есть возможность ручного тестирования через expo app. Но поддерживается ли тут playwright тестирование e2e тестов. Это только обсудить и рассказать.
