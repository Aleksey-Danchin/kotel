entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  websecure:
    address: ":443"
  api1:
    address: ":3001"
  api2:
    address: ":3002"
  ping:
    address: ":8082"

providers:
  docker:
    exposedByDefault: false
    network: kotel-net
  file:
    directory: /etc/traefik/dynamic
    watch: true

api:
  dashboard: false

ping:
  entryPoint: ping

log:
  level: WARN
